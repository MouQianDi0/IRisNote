/** Shared border radii that mirror the semantic radius tokens in global.css. */
export const radius = {
    indicator: 6,
    control: 12,
    card: 14,
    modal: 16,
    floating: 18,
    profile: 32,
    full: 9999,
    hyperModal: 24,
    hyperCard: 16,
    hyperControl: 14,
} as const;
