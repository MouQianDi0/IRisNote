# This file is a template, never put real secrets in it. Load values into the
# calling shell or a CI secret store. The CLI does not automatically source files.
IRIS_RELEASE_API=https://example.com/api/releases
IRIS_RELEASE_TOKEN=
IRIS_CERTIFICATE_SHA256=
ANDROID_HOME=D:\AndroidSDK
JAVA_HOME=
IRIS_KEYSTORE_PATH=
IRIS_KEYSTORE_PASSWORD=
IRIS_KEY_ALIAS=
IRIS_KEY_PASSWORD=
EXPO_PUBLIC_BASE_URL=https://example.com/api
EXPO_PUBLIC_RELEASE_API_URL=https://example.com/api/releases

# Optional explicit paths; otherwise run release setup-delta first
IRIS_HDIFFZ_PATH=
IRIS_HPATCHZ_PATH=
