# 2026-09-27 合并 origin/master（e857a52）并修复 CHANGELOG 上次合并截断

## 1. 改动清单（按层）

- 合并范围：origin/master 的 `d0a8eaf..e857a52` 共 20 个提交合入 `kroos_todo`（417 个文件，+41047/−14998），按层汇总：
  - `src/features/`：excerpts 新域（本地摘录库、剪贴板自动检测与 HMAC 已处理记录）、notes（15 天回收站、草稿管理重构、阅读进度迁 SQLite、历史版本保留 50 条）、profile（改邮箱/改密码、地区选择、头像缓存）、settings（数据与存储页、发布历史服务端拉取）、todos（动态卡服务化、live update 协调器）；
  - `src/core/`：database 迁移 0012–0015、storage 自动清理与策略、diagnostics 扩展、system-notifications 重构、cloud-storage 授权门控、providers 新增 StartupMaintenance；
  - `src/shared/`：http 会话事件、secure-store/token-storage、ui 新增 ListRow/PageHeader/AppBrandIcon、useTransitionLock；
  - `modules/irisnote-system/`：`android/.../live/` 新增 5 个 Kotlin 文件（LiveTodoAlarmReceiver/ForegroundService/Notifier/Scheduler/Timeline）与模块 API 扩展；
  - `tests/`：新增 24 个 `.cjs` 测试文件；`docs/`、`releases/`、`scripts/release/`、`.prettierrc` 等配套更新。
- 本次合并自身新增改动（非带入）：`CHANGELOG.md`（冲突解决 + 截断修复，见第 2 节）、`docs/logs/2026-09-27-merge-master-e857a52.md`（本篇）。无业务代码新写。

## 2. 与原代码对比

- 基点：merge-base `d0a8eaf`（PR #123 合入点）；`kroos_todo` 侧 18 个独有提交，master 侧 20 个独有提交，属双向分叉合并。
- 唯一内容冲突文件：`CHANGELOG.md`（双方均在顶部追加条目；其余 416 个文件全部自动合并，含 `diagnostic-log.ts`、`PermissionSettingsScreen.tsx` 两处自动合并成功项）。
- 原来是什么：`kroos_todo` 的 `CHANGELOG.md` 在上次合并提交 `5143765`（合入 PR #123）中被意外截断为 29 行、仅 4 条记录——17 条 kroos 独有条目丢失，其中 `2026-09-26 06:40:00`（逐条待办卡按重要度提升）条目只剩一行正文；master 侧完整保有 266 条。
- 现在是什么：`CHANGELOG.md` 重建为两侧并集共 283 条 = master 266 条全量（条目内部顺序未改动）+ 从 `5143765^1`（截断前的完整版，259 条）找回的 kroos 独有 17 条，按条目时间戳插入对应时间位置。
- 行为反转/默认值开关：无。本合并未改任何业务默认值；带入功能的行为以其各自提交与记录为准。

## 3. 改动原因

- 用户要求把主分支更新合入 `kroos_todo`。
- 解决 `CHANGELOG.md` 冲突时发现：简单「双方顶部拼接」会延续上次合并造成的数据丢失（17 条记录缺失、1 条不完整），故改为从 `5143765^1` 找回完整 kroos 条目再并入，恢复分支历史记录的可追溯性。

## 4. 完整调用链路

- 本次为分支级合并，无新增运行时链路。操作链路：
  `git fetch origin` → 本地 `master` fast-forward（`fa30de4`→`e857a52`）→ `git merge master`（仅 CHANGELOG.md 冲突）→ 以 `:3:`（master stage）全量条目 + `5143765^1` kroos 独有条目按时间序并集重建 CHANGELOG.md → `git add` → `npm install`（安装带入依赖 expo-secure-store ~57.0.4、expo-crypto ~57.0.3）→ `npm run check` → 合并提交。
- 带入的运行时功能链路（摘录、回收站、令牌 SecureStore、后台实时刷新、云存储门控等）见 master 侧各自 CHANGELOG 条目与提交记录，本篇不重复；其中通知/闹钟相关原生改动如需真机验证，须重新构建原生包。

## 5. 验证情况

- 合并前（`kroos_todo` @ `5143765`）：`npm run typecheck` 0 错误。
- 合并解决后首次 `npm run check`：typecheck 报 4 个 TS2307（`expo-crypto`、`expo-secure-store` 模块不存在）——原因为带入的新依赖尚未安装，属环境问题而非代码冲突；`npm install` 后消除。
- 合并后 `npm run check` 全部通过：类型检查 0 错误、Lint 0 问题、`theme:check` 通过、`node --test` 593 通过 / 0 失败 / 0 跳过（此前 CHANGELOG 记录的「发布归档 ENAMETOOLONG」既有失败本次未出现）。
- 未做真机验证（带入功能是否需要真机验收，以各自条目的「验证」说明为准）；未推送远程。
