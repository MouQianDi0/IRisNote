# IRisNote 通知渠道适配（Android / iOS / 鸿蒙）

> 文档版本：1.3（2026-09-23 夜：动态通知升级为请求 Live Updates 提升式（上岛），§0/§6.2 更新；1.2：Android 16 动态通知 ProgressStyle 立项落地，§0 决策反转）
> 状态：**本地通知与 Android 16 动态通知代码已实现，真机送达待验收**；远程推送、鸿蒙仍为调研。iOS 自定义 threadIdentifier 分组受当前 SDK 调度输入限制，未实现。
> 关联文档：[全局横幅通知设计与调用规范.md](./全局横幅通知设计与调用规范.md)（应用内横幅层，已实现）、[IRisNote视觉设计规范.md](./IRisNote视觉设计规范.md)（§设置页立项约束）、[日历公共组件规范.md](./日历公共组件规范.md)（首版不做日程，划界依据）、[../待办/TODO.md](../待办/TODO.md)、[../架构指南/后续开发指南.md](../架构指南/后续开发指南.md) §14

---

## 0. 文档定位与边界

- **定位**：保留三平台能力调研；§6–7 记录当前 Android/iOS 本地通知实现，其他平台能力不代表已接入。
- **三层通知边界**（避免混淆）：
  1. **应用内横幅**（已实现，`src/core/notifications/`，见全局横幅规范）——纯 JS 层 UI，与系统通知无关，本文档**不改动**该层；
  2. **系统通知**（本文档主题）——`src/core/system-notifications/` 已独立接入；首个场景为待办开始提醒；
  3. **邮件渠道**（`resend` 依赖，属验证码类邮件）与系统通知无关，不在本文范围。
- **结论先行（动态通知）**：初版结论"动态通知暂不立项"已于 **2026-09-23 经用户指令反转**，同日夜间再升级为**请求提升式（promoted）**：声明 `POST_PROMOTED_NOTIFICATIONS`、经反射调用 `setRequestPromotedOngoing(true)`（compileSdk 36.0 无该 36.1 符号，36.0 设备退化为普通进度卡片）；提升式仅覆盖正在进行的真实待办和 60 秒模拟待办，**普通提醒、即将到来的日历事件仍禁入**。已落地场景见 §6.2；iOS Live Activities 与鸿蒙实况窗维持不立项，§5 认知映射保留。

---

## 1. 三平台通知体系总览

### 1.1 按本文三分类的落地形态

| 分类 | Android（8.0+ / 16） | iOS（15+ / 16.1+） | 鸿蒙 HarmonyOS NEXT |
|---|---|---|---|
| **普通通知** | 渠道 importance `DEFAULT`（有提示音、无横幅）或 `LOW`（静默、仅图标） | interruptionLevel `active`（默认行为）/ `passive`（无声入列表，不打扰） | 渠道 `CONTENT_INFORMATION` / `OTHER_TYPES`（默认 LEVEL_MIN，静默）或 `SERVICE_INFORMATION` 调低 |
| **重要通知** | 渠道 importance `HIGH`（横幅 heads-up + 提示音 + 振动）；**无更高等级** | interruptionLevel `timeSensitive`（时效性，突破专注模式/定时摘要）；`critical`（需 Apple 特批 entitlement，穿透静音） | `SERVICE_INFORMATION` / `SOCIAL_COMMUNICATION` 默认 LEVEL_HIGH（横幅 + 提示音；横幅最终以用户设置开关为准） |
| **动态通知** | 前台服务常驻通知（MediaStyle/导航等）+ **Android 16 Live Updates**（QPR1 起完整生效） | **Live Activities**（灵动岛 + 锁屏实时活动，ActivityKit，16.1+） | **实况窗 Live View Kit**（状态栏胶囊 + 锁屏/通知卡片，HarmonyOS 5+） |

### 1.2 渠道概念映射

| 概念 | Android | iOS | 鸿蒙 |
|---|---|---|---|
| 开发者建"渠道" | `NotificationChannel`，**任意数量、自定义 ID/名称/图标** | **无用户可见渠道**；按通知携带 Category（动作组）、`threadIdentifier`（分组）、`interruptionLevel`（分级） | `NotificationSlot`，**固定系统枚举 7 类，不可自建任意命名渠道** |
| 分级档位 | importance 5 档（NONE/MIN/LOW/DEFAULT/HIGH） | interruptionLevel 4 档（passive/active/timeSensitive/critical） | SlotLevel 5 档（NONE/MIN/LOW/DEFAULT/HIGH） |
| 分级归属 | **渠道级**（建渠道时定死，建后 App 改不动） | **单条通知级**（每条通知自带） | 渠道类型映射默认级别，可经 `addSlot` 调整，用户设置优先 |
| 用户控制粒度 | 每渠道：声音/振动/横幅/锁屏/角标全可改 | 每 App：锁屏/横幅/声音/标记/预览/时效性整体开关 | 每渠道类型 + 每应用：横幅/声音等在 设置>通知和状态栏 |

---

## 2. Android：通知渠道（NotificationChannel）

### 2.1 渠道模型

- API 26+ 通知**必须**挂渠道；App 可创建任意数量的 `NotificationChannel`（自定义 ID、用户可见名称、描述、图标，可用 `ChannelGroup` 分组）。
- 渠道创建时可设置：importance、声音（含 `audioAttributes`）、振动样式、呼吸灯、锁屏可见性、角标、`bypassDnd` 等；**创建后 App 再改 importance 会被系统忽略**（只能换新渠道 ID 迁移，且用户对该渠道的设置随之丢失）。
- 用户在系统设置中可覆盖渠道的一切属性（降级、关声音、关横幅），App 只能读（`getNotificationChannelAsync`）不能写。
- Android 13+：`POST_NOTIFICATIONS` 运行时权限，未授权则一切通知不显示。

### 2.2 普通通知

| importance | 行为 | 适用 |
|---|---|---|
| `LOW`（2） | 无声、无横幅，仅状态栏图标+下拉列表 | 同步结果、后台任务完成等低打扰事件 |
| `DEFAULT`（3） | 有提示音，无 heads-up 横幅 | 一般服务消息、公告 |

### 2.3 重要通知

- `HIGH`（4）= **Android 第三方应用的打扰上限**：heads-up 横幅 + 提示音 + 振动。
- **没有 iOS critical 那样的穿透等级**：`bypassDnd` 对第三方应用基本无效，勿打扰模式下无法保证送达；"重要"最终由用户对渠道的设置决定。
- 长按通知可直达渠道设置，用户可把 HIGH 一键降成静音——设计上应把"重要"用于少数渠道，而非单条通知。

### 2.4 动态通知

两种形态：

1. **前台服务常驻通知**（传统形态）：`MediaStyle`、通话 `CallStyle`、导航类 FGS 的进度通知，`setOngoing(true)` 不可滑删，系统级保障存活。适合"正在进行的任务"，但展示形态仍是普通通知卡片。
2. **Live Updates**（Android 16 引入、QPR1 起完整生效，对标 iOS 灵动岛）：通知被"提升（promoted）"到抽屉顶部置顶、锁屏常驻、状态栏**胶囊 chip**（最大宽 96dp，可含图标+短文本+倒计时），默认展开、不可折叠，用户可临时收起或降级为普通通知。

**Live Updates 硬性要求**（全部满足才可被提升）：

| 项 | 要求 |
|---|---|
| 样式 | Standard / BigTextStyle / **CallStyle** / **ProgressStyle**（新，进度段+节点+计时器）/ MetricStyle |
| 权限 | Manifest 声明 `android.permission.POST_PROMOTED_NOTIFICATIONS`（安装级，非运行时）；用户总开关可用 `canPostPromotedNotifications()` 查询 |
| 请求提升 | `setRequestPromotedOngoing(true)`（或 `EXTRA_REQUEST_PROMOTED_ONGOING`）+ `setOngoing(true)` + 必须有 `contentTitle` |
| 禁止项 | 自定义 RemoteViews 布局、group summary、`setColorized(true)`、渠道为 `IMPORTANCE_MIN` |
| 政策准入 | 必须是**用户主动发起、正在进行、全程需要关注**的"旅程"（导航、通话、打车/外卖跟踪） |
| 政策禁止 | **广告/促销、聊天消息、普通提醒、即将到来的日历事件**、功能快捷入口（此类应使用 Widget/快捷设置磁贴） |
| 生命周期 | 任务结束必须立即转普通通知或撤下；被用户降级后**禁止**重发提升通知（违规会导致权限被回收） |
| 其他 | target SDK 36；各 OEM 可附加准入条件 |

### 2.5 自定义程度评估

- **渠道维度自由度三平台最高**：任意命名/图标/数量、渠道级声音+振动样式+呼吸灯+锁屏策略。
- **代价是"创建即写死"**：importance 与声音等一旦发布不可修改，迭代需换渠道 ID 并承接用户流失。
- 用户主权最高：每渠道全属性可改；OEM（HyperOS/ColorOS 等）还会叠加自启动、通知分类等管控层，真机表现需按 [adb-layout-dump] 流程实测。

---

## 3. iOS：无渠道模型，分级随单条通知走

### 3.1 模型

- 授权制：系统设置按 App 控制锁屏、横幅、声音、标记和预览，没有渠道级 UI。SDK 57 的 `IosNotificationPermissionsRequest` **没有 timeSensitive 字段**；本期只申请 alert/sound，明确不申请 badge、critical 或 provisional。
- App 侧组织手段：`UNNotificationCategory`（长按/下拉的动作按钮组，可含文本输入）、`threadIdentifier`（会话分组折叠）、`relevanceScore`、自定义声音（打包内 ≤30s）、Notification Service Extension（送达前解密/下载附件）。

### 3.2 普通通知

- `active`：默认，正常横幅+声音。
- `passive`（iOS 15+）：无声、无横幅、无触感，仅出现在通知列表/锁屏——适合"同步完成""已保存"类回执。

### 3.3 重要通知

- `timeSensitive`（时效性）属于另行评估的原生能力；不得把通知内容的 `interruptionLevel` 当作权限申请字段。本期只使用 `active`，不声明支持穿透专注模式。
- `critical`（关键警报）：需向 **Apple 单独申请 entitlement**（医疗、安防、无障碍等特定品类），可穿透静音键与勿打扰、强制音量。**笔记类应用不可能获批 → 本项目不做**。

### 3.4 动态通知：Live Activities

- iOS 16.1+，ActivityKit 启动/更新/结束；展示于**灵动岛**（iPhone 14 Pro 起）与**锁屏卡片**，锁屏后仍持续更新。
- 更新途径：App 进程内直接更新；或 APNs 推送（push type `liveactivity`，设备级 token，`timestamp`/`staleDate` 携带）。**推送更新有动态预算限频**，只适合分钟级以上粒度，不适合秒级刷新。
- 生命周期：活跃最长 **8 小时**（此后系统强制结束、仅保留锁屏展示），**12 小时**后彻底移除。
- 支持锁屏卡片上的按钮交互（App Intents）；内容需为"持续变化的实时数据"（进度、比分、配送位置）。

### 3.5 自定义程度评估

- **通知本体**自定义强：附件、动作按钮、分组、声音（≤30s）、Service Extension 二次加工。
- **渠道维度无**：分级、声音意图由每条通知自带；`critical`/`timeSensitive` 需 Apple 侧 capability/entitlement 门槛。
- 策略空间：`provisional` 静默授权（先静默送达、在通知列表里让用户升级为正式授权）适合提醒类场景的弱打扰起步。

---

## 4. 鸿蒙 HarmonyOS NEXT：固定渠道 + 实况窗白名单制

### 4.1 模型（Notification Kit）

- `notificationManager.publish(request)` 发布；`request.notificationSlotType` 指定渠道（**渠道不存在会按默认配置自动创建**；社区经验：务必把 slotType 写进 request，仅 `addSlot` 不保证生效）。
- 授权：`requestEnableNotification(context)` 拉起系统弹窗——**每个应用只弹一次**，用户拒绝后无法再弹，需 `openNotificationSettings`（API 13+）引导去设置；`isNotificationEnabled(Sync)` 查询状态。
- **渠道是系统固定枚举，第三方不能自定义命名渠道**（与 Android 最大的差异）。

### 4.2 SlotType 枚举（渠道类型 × 默认级别）

| SlotType | 值 | 含义 | 默认 SlotLevel | 备注 |
|---|---|---|---|---|
| `UNKNOWN_TYPE` | 0 | 未知 | LEVEL_MIN | |
| `SOCIAL_COMMUNICATION` | 1 | 社交通信 | **LEVEL_HIGH**（横幅+提示音） | 面向 IM 类；笔记应用不宜占用 |
| `SERVICE_INFORMATION` | 2 | 服务提醒 | **LEVEL_HIGH**（横幅+提示音） | **本项目提醒类的正确落点** |
| `CONTENT_INFORMATION` | 3 | 内容资讯 | LEVEL_MIN（静默） | 低打扰回执类 |
| `LIVE_VIEW` | 4（API 11+） | 实况窗 | LEVEL_DEFAULT | **三方应用不可直接创建**，见 §4.4 |
| `CUSTOMER_SERVICE` | 5（API 11+） | 客服消息 | LEVEL_DEFAULT | 需用户主动发起会话 |
| `OTHER_TYPES` | 0xFFFF | 其他 | LEVEL_MIN | 兜底 |

SlotLevel 档位：`LEVEL_NONE`(0) 关闭 / `LEVEL_MIN`(1) 无图标无声 / `LEVEL_LOW`(2) 有图标、无横幅无声 / `LEVEL_DEFAULT`(3) 有图标+提示音、无横幅 / `LEVEL_HIGH`(4) 有图标+**横幅**+提示音。横幅展示最终以用户在 设置>通知和状态栏 的开关为准。

### 4.3 普通 / 重要通知

- **普通**：`CONTENT_INFORMATION` / `OTHER_TYPES`（默认静默入通知中心），或将 `SERVICE_INFORMATION` 经 `addSlot` 调低。
- **重要**：`SERVICE_INFORMATION`（默认 LEVEL_HIGH = 横幅+提示音），即鸿蒙上"重要通知"不需要像 Android 那样建专属渠道，**选对渠道类型即得默认强度**；强度上调空间止于 LEVEL_HIGH，无 critical 等价物，勿打扰穿透受系统/用户管控。

### 4.4 动态通知：实况窗（Live View Kit）

- 展示形态：状态栏**胶囊**（灵动岛式）+ 锁屏/通知卡片 + 沉浸态（地图、计时等富内容）；系统应用（通话/闹钟/录音/计时等）与三方应用两套体系，**三方需走 Live View Kit**（`liveViewManager.startLiveView`），`LIVE_VIEW` 渠道由**系统代理创建**，三方只能按同 ID 发布更新。
- **场景准入（白名单制）**：须满足时段性（明确起止）、时效性（信息只在特定时段有价值）、变化性（内容持续更新）；官方 11 类模板场景：即时配送、打车、航班、高铁、排队叫号、赛事比分、导航、计时、共享租赁、取餐、运动锻炼——**无笔记/日历类场景**。
- 接入门槛：AppGallery Connect「项目设置>开放能力管理」**申请实况窗权限**（按场景审核，需符合实况窗设计规范）；用户侧还需在 设置>通知和状态栏 开启实况窗显示。
- 生命周期：单个实况窗**最长 8 小时**（超时系统强制结束）；6.1.1(24) 起可用 `aliveTime` 自动关闭；系统要求及时更新保证时效。
- 更新方式：本地更新依赖应用进程存活，**官方推荐 Push Kit 推送更新**；HarmonyOS 5+，仅 Phone/Tablet。

### 4.5 自定义程度评估

- 三平台**最受限**：渠道不可命名/不可自由创建，级别由渠道类型映射；通知内容（文本/图片/多行/进度条）本身可自定义。
- 实况窗形态最像 iOS 灵动岛，但准入是**审核制**而非开发者自治，笔记类应用无法使用。

---

## 5. 横向对比与自定义程度总表

| 维度 | Android | iOS | 鸿蒙 NEXT |
|---|---|---|---|
| 渠道模型 | 任意自建渠道（Android 最自由） | 无渠道，分级随单条通知 | 固定 7 类系统枚举 |
| 分级 | 渠道级 importance（建后锁死） | 单条 interruptionLevel | 渠道类型映射 SlotLevel |
| 重要级打扰上限 | HIGH（heads-up） | timeSensitive / critical（特批） | LEVEL_HIGH（横幅+提示音） |
| 穿透勿打扰 | 第三方不可 | timeSensitive 可（用户可关）；critical 可 | 受系统/用户管控，无保证 |
| 动态形态 | FGS 常驻 + Live Updates（QPR1+） | Live Activities（16.1+） | 实况窗（HarmonyOS 5+） |
| 动态准入 | 开发者自治 + 政策约束（禁日历事件/广告） | 开发者自治（需持续变化内容） | **AGC 场景白名单审核** |
| 动态时长上限 | 任务结束即止 | 活跃 8h / 移除 12h | 8h（aliveTime 可自动关） |
| 声音自定义 | 渠道级，任意音频+audioAttributes | 单条，打包内 ≤30s | 渠道属性可设，系统声为主 |
| 授权机制 | 13+ 运行时权限 | 首版申请 alert/sound；SDK 57 无 timeSensitive 请求字段 | 弹窗仅一次，拒绝后引导设置 |
| 用户控制粒度 | 每渠道 | 每 App（+时效性总开关） | 每渠道类型+每应用 |
| OEM/厂商变数 | 高（国内 ROM 叠加管控、无 FCM） | 无 | 低（华为统一） |

**对本项目的含义**：同一业务语义需要三套落地——"提醒类"在 Android 走 HIGH 渠道、在 iOS 走 timeSensitive/active、在鸿蒙走 `SERVICE_INFORMATION`；"回执类"在 Android 走 LOW、iOS 走 passive、鸿蒙走 `CONTENT_INFORMATION`。**不存在三平台统一的渠道抽象**，只能以"业务语义 → 各平台映射"表驱动。

---

## 6. IRisNote 通知需求汇总（现状与出处）

### 6.1 实现现状：本地系统通知与待办开始提醒

- `expo-notifications ~57.0.20`、配置插件、白色透明通知小图标已接入。Android 原生目录受 `.gitignore` 管理，通过预构建生成；通知库合并 `POST_NOTIFICATIONS` / 重启恢复声明，不申请精确闹钟权限。
- 已实现的 `src/core/notifications/`（8 个文件 + 回归测试）是**应用内全局横幅**（banner.show/resolve + critical 优先级 + until-resolved 生命周期），不触碰系统通知。
- 设置页显示系统通知已开启/未开启并进入系统设置；待办表单增加 48dp 提醒行（上下间距 12dp、水平内边距 16dp、文字与开关 gap 12dp），无开始时间时原生 Switch 关闭且禁用。
- 全局 Provider 在认证归属就绪后加载待办，启动和前台恢复对账；仓库提交后订阅覆盖保存、完成、取消完成、删除和账号切换。
- 迁移 0008 的独立 `todo_reminder_bindings` 表保存 owner/todo、系统 ID、版本、触发时间、schedule/scheduled/cancel/error 状态、最近错误与更新时间。无级联外键，删除后仍保留取消重试记录。
- 调度前持久化确定性 ID，成功后记录系统返回 ID；串行核对 SQLite 事实、绑定和系统队列，清理孤儿请求。取消失败禁止重建，调度失败不回滚待办。失败在后续启动/前台/待办变更时重试。
- 通知点击等待认证与根导航就绪，仅导航待办页对应日期；旧账号或已删除待办只进入列表并提示，不打开编辑弹窗、不展示其他账号摘要。

### 6.2 实现现状：Android 16 动态通知（ProgressStyle，2026-09-23 立项）

- **决策依据**：用户 2026-09-23 指令反转 §0 初版结论（原"暂不立项"），先落地 Android 进度式动态通知；**同日夜间再次经用户指令升级为请求提升式 Live Updates**——声明 `POST_PROMOTED_NOTIFICATIONS` + 反射 `setRequestPromotedOngoing(true)`（compileSdk 36.0 无该 36.1 符号），提升式仅覆盖进行中任务/计时器两类动态卡片，"普通提醒/即将到来的日历事件"仍不提升（政策禁入不变）；生态 alpha 库（expo-live-updates 0.1.0-alpha3）仍不引入。
- **原生层**：`modules/irisnote-system`（Expo Modules API）新增 `postProgressNotification` / `cancelProgressNotification`：`Notification.ProgressStyle` + `CATEGORY_PROGRESS` + `setOnlyAlertOnce(true)`，同一整型通知 ID 经 `NotificationManager.notify` 原位更新；无 segments 时进度按 `progress/max` 折算百分比。SDK < 36 抛"需要 Android 16"；点击通知仅启动应用主界面（v1 不做深链路由）。`promoted=true`（JS 层默认开）时强制 `setOngoing(true)` 并反射请求 `setRequestPromotedOngoing(true)` 上岛；36.0 设备无该符号静默退化为普通进度卡片。
- **渠道**：`irisnote.live-todo.v1`（"待办进行中"，LOW，静默），由 `system-notification.service.ts` 单点创建、进程内记忆一次。旧安装包可能保留历史 `live-test.v1` 渠道，但新测试不再使用。
- **设置页演示**：帮助与反馈 → 诊断与排障 → "测试待办动态通知"：创建仅在内存存在的 60 秒模拟待办，走与真实待办相同的协调器、ProgressStyle 通知和退后台原生时间线；不写入待办或同步数据，不占真实待办三张卡额度；结束或离开页面撤下，诊断 scope 为 `live_update`。
- **待办接入**：`todo-live-update.service.ts` 纯函数（资格 = 当天 + reminderEnabled + 未完成 + 有开始时刻 + 进行中窗口，与列表状态口径一致；进度分钟级；无结束时间用不定进度；标题复用提醒摘要脱敏截断；FNV-1a 生成稳定整型通知 ID）+ `TodoLiveUpdateCoordinator`（前台 30 秒例行差量刷新、待办仓库变更即刷、退后台移交时间线（见下）/Provider 卸载全撤、同屏上限 3 条按通知 ID 确定性截断），挂载于全局 SystemNotificationProvider，权限未授予即不发并撤卡。
- **后台实时刷新（2026-10 立项，三层组合）**：
  - **方案 A（默认，无需授权）**：退后台时 JS 把当日时间线快照（进行中 + 稍后开始的合格待办，`desiredTodoLiveTimelines`，**进行中优先、组内按通知 ID 确定性截断**——退后台不因未来卡挤掉进行中卡）移交原生 `scheduleLiveTodoCards`；原生 `LiveTodoScheduler` 单闹钟链（`setAndAllowWhileIdle` 非精确，Doze 下允许合并延迟）按墙钟差量刷新（分钟级），进程被杀后从 SharedPreferences 恢复续算，全部结束自动停摆；回前台 `cancelScheduledLiveTodoCards` 收回接管权（不动通知，JS 差量原位对账）；**收回失败时 start 放弃本轮接管**（保持原生分钟级驱动，避免 JS 与闹钟双驱动）；移交在途被抢先回前台/停机时补撤销，移交失败撤销原生接管并逐卡撤下（不留冻结错误进度）。
  - **方案 C（默认，随 A 生效）**：通知构建统一带 chronometer 锚点（有 endAt 倒计时 / 无 endAt 正向计时），系统每秒自动刷新时间戳区域零唤醒；在 ProgressStyle/提升式岛上的渲染以真机验收为准，36.0 与 36.1 框架差异最坏退化为不显示计时。
  - **方案 B（用户开关）**：设置 → 权限设置 → 「后台实时刷新」（仅 Android 16+ 可用，`live_todo_realtime_enabled` 偏好键）；**数据流：JS 在前台启动 FGS 前先经 `updateLiveTodoCards` 持久化时间线快照（不排闹钟），FGS 每秒从快照重算，JS run() 每轮刷新快照使编辑/完成 1 秒内反映**；前台服务（`specialUse` + FOREGROUND_SERVICE(_SPECIAL_USE) 权限）每秒重算原位更新（进度条秒级平滑），FGS 通知即进度卡本身；FGS 空快照或无进行中卡时**安全停机**（先占位 startForeground 再移除停止，规避 startForegroundService 未配套 startForeground 的系统崩溃，不再提前展示未来卡）；开关关闭停止 FGS 后 JS 下一轮强制原位重发锚点卡；方案 A 闹钟始终并存兜底——FGS 被系统回收自动降级分钟级。
- **边界**：退后台**保留**卡片并移交原生续算（时间线为快照——后台期间他端完成/删除待办不反映，回前台 reconcile 校正）；设备重启后不自启（不加 RECEIVE_BOOT_COMPLETED，重启后卡片消失，下次打开重建）；Android 12+ 禁止后台启动前台服务——FGS 仅由 JS 在前台（开关开启且有活跃卡）启动（persist→start 两步间隙退后台则本次启动被系统拒绝，降级为纯方案 A 分钟级），退后台后到点才开始的新卡停留分钟级（A）；被杀后下次启动由冷启动清理（`clearStaleLiveUpdates`：按 live-todo 渠道整清 + 无条件撤演示 ID）撤除残留；后台到点仍由既有普通开始提醒兜底（形态不升级，与进行中卡片在到点瞬间可能并存，提醒横幅有声、进行中卡片静默）；API < 36 或 Expo Go 不显示且入口禁用（能力位经 context 下发 `liveUpdateCapable`，设置页不静态加载通知原生模块）；**上岛需 36.1+ 设备且系统设置"实时更新"开启，36.0 退化普通进度卡片；用户降级后政策禁止重发提升式**；小米焦点通知/OPPO 流体云等厂商私有岛未单独接入，走标准 Live Updates 路径由各 OEM 系统自行决定映射，渲染差异待真机验收；国产 ROM 对闹钟广播/前台服务的后台管控差异待真机验收。

### 6.3 其他需求来源（历史调研，实际状态以 §6.1–6.2 为准）

| # | 需求表述 | 出处 | 性质 |
|---|---|---|---|
| 1 | Settings 功能接入：**通知**设置的类型、本地存储与子页面 | `docs/待办/TODO.md` L72；`docs/架构指南/后续开发指南.md` §14（落位方案） | 已排期方向 |
| 2 | 设置页"通知：站内"概览与"通知设置"入口 | `docs/UI/IRisNote视觉设计规范.md` L255-262（含**"系统通知必须独立立项"**硬约束）；`README.md` L17 | UI 已预留 |
| 3 | 服务端"消息推送"扩展 | `docs/API后端/IRisNote 服务端部署手册（Docker 生产版）.md` §19 未来扩展 | 远期 |
| 4 | AI 日程/排期愿景（"/日历 命令 + AI 日程"，"下个工作日""避开假期"类指令） | `docs/学习参考/节假日数据源调研与选型.md` L140-156 | 业务愿景，是**提醒类通知**最可能的数据来源 |
| 5 | 日历组件首版**不做**日程/agenda | `docs/UI/日历公共组件规范.md` L17（"出现需求时另行立项扩展"） | 负向约束：日程提醒非近期 |
| 6 | 应用内横幅体系（已实现）与系统通知并行分层 | `docs/UI/全局横幅通知设计与调用规范.md`（"系统权限弹窗等系统界面不在应用层级保证范围内"） | 分层依据 |

**排除的干扰语义**（调研时勿混淆）：编辑器架构文档中的"增量推送"= 数据同步；事件总线/Zustand listener = 代码订阅；验证码 60s 倒计时 = 应用内计时。

### 6.4 分层结论

- 前台使用中的提示 → 应用内横幅（已有体系，继续用）；**后台/杀进程后仍需触达**（提醒到期、远期服务端推送）→ 系统通知，二者并行不互替。
- 首个已立项场景为待办开始提醒，不依赖系统日历或远程推送；前台也显示系统通知，不叠加应用内成功横幅。

---

## 7. 适配方案草案（渠道映射 + Expo 落点）

### 7.1 预设渠道映射表（拟）

| 业务语义（拟） | Android 渠道（importance） | iOS（interruptionLevel） | 鸿蒙（slotType） | 说明 |
|---|---|---|---|---|
| **待办开始提醒**（已实现） | `irisnote.reminders.v1` — **HIGH**；默认声音/振动、PRIVATE、无角标 | `active`；不启用时效性或 critical | 未实现 | 是否横幅/有声以系统和用户设置为准 |
| **回执**（同步完成/保存结果，后台触达时） | `sync` — **LOW** | `passive` | `CONTENT_INFORMATION`（默认 MIN 静默） | 低打扰；多数场景仍由应用内横幅承担 |
| **服务端消息**（远期 irisapi 推送） | `general` — DEFAULT | `active` | `SERVICE_INFORMATION`（经 addSlot 调 DEFAULT/LOW） | 远期立项时再细化 |
| **模拟待办动态通知**（已实现） | `irisnote.live-todo.v1` — LOW；静默、PRIVATE、无角标 | 不适用（Android 专属演示） | 不适用 | 设置页 60 秒模拟待办；共用真实待办的 ProgressStyle 和后台时间线 |
| **待办进行中**（已实现） | `irisnote.live-todo.v1` — **LOW**；静默、PRIVATE、无角标 | 不适用 | 不适用 | 前台动态进度卡片，上限 3 条（见 §6.2） |

### 7.2 Expo SDK 57 落点（立项时的技术底座）

- **expo-notifications 已安装**，仅创建 reminders 渠道，sync 只保留语义，不提前创建。SDK 57 已核实能力：
  - Android 渠道全套 API：`setNotificationChannelAsync(channelId, { name, importance, sound, vibrationPattern, enableVibrate, enableLights, lightColor, bypassDnd, lockscreenVisibility, showBadge, ... })`；注意 **importance 建后不可改**，渠道 ID 一次定准。
  - iOS `interruptionLevel` **直接支持** `'passive' | 'active' | 'timeSensitive' | 'critical'`（后两者还需 capability/entitlement）。
  - 本地调度：`scheduleNotificationAsync` + `SchedulableTriggerInputTypes`（DATE/DAILY/WEEKLY/CALENDAR…），提醒类纯本地通知即可覆盖，不依赖推送服务。
  - 自定义声音需在 app.config 通过 config plugin `sounds` 数组注册。
  - Android 推送（若远期接 expo push）**SDK 53 起必须 dev build**，Expo Go 不可用。
- **Live Activities 不在 expo-notifications 能力内**，需第三方库 + config plugin（iOS 侧暂无场景，仅记录）。**Android 16 动态通知同样不在 expo-notifications 能力内**（v57 已核实无 ProgressStyle/LiveUpdates 输入），已由自有模块 `modules/irisnote-system` 原生实现（见 §6.2），不引入生态 alpha 库（expo-live-updates 要求 API 36.1 + `POST_PROMOTED_NOTIFICATIONS`）。
- **鸿蒙无 Expo 构建目标**：鸿蒙列按 §4 记录的纯能力调研处理；若未来做鸿蒙端，需 react-native-harmony 适配层或原生实现，届时渠道映射直接查 §7.1 表。
- iOS Bundle ID 配置为 `com.mouqiandi.irisNote`，未核实 Apple 注册/签名。`with-local-notification-entitlements` 放在官方通知插件前（mod 逆序执行），移除插件默认添加的 `aps-environment`；无 APNs token、远程后台模式或推送服务接入。
- iOS `threadIdentifier` 在当前 SDK 仅可从通知结果读取，本地输入类型及原生构造未暴露。首版不通过类型断言伪造该功能，也不补写 node_modules；自定义分组未实现。

### 7.3 权限申请时机（已实现）

- 启动只创建 Android 渠道并查询权限。明确确认保存未来开始且开启提醒的待办后，才请求系统权限；无开始、过去时刻或已完成不申请。
- 遮罩/返回自动保存不弹系统权限。未授权时提示“待办已保存，系统提醒尚未开启”，用户点击“开启”后申请；不可再请求则引导系统设置。拒绝不影响已保存实体。
- 设置页直达系统应用通知设置，回到前台刷新状态与对账。iOS provisional/ephemeral 视为允许调度，不承诺有声横幅；Android 也检查提醒渠道是否关闭。
- 日期和时间按设备当前民用时区解析，前台恢复时重新计算；DST 缺失分钟报错，不静默进位；重复小时采用 JavaScript Date 的较早时刻。摘要最多 100 个码点，去除控制字符；不读取关联笔记、凭据或服务端错误。

### 7.4 风险清单

| 风险 | 影响 | 对策 |
|---|---|---|
| Android 14+ 精确闹钟收紧（`SCHEDULE_EXACT_ALARM` 默认拒发，`USE_EXACT_ALARM` 限闹钟/日历品类） | 提醒可能延迟数分钟 | 首版不声明精确闹钟权限，接受系统延迟 |
| 用户降级渠道、专注模式、OEM 省电和强制停止应用 | 不保证准点、有声或横幅 | 尊重系统设置；不重复叠加站内成功通知，不宣称必达 |
| 国内 Android 无 FCM，OEM 推送 SDK 各异 | 仅影响**远期服务端推送**（§6.2-3），本地提醒不受影响 | 远期立项时再评估厂商通道/聚合服务 |
| 鸿蒙授权弹窗仅一次 | 拒绝后永远要靠引导 | 首弹文案谨慎；设置页提供"去开启"入口 |

---

## 8. 参考资料

- Android 通知渠道：<https://developer.android.com/develop/ui/views/notifications/channels>
- Android 16 Live Updates：<https://developer.android.com/develop/ui/compose/notifications/live-update> · 进度型通知：<https://developer.android.com/about/versions/16/features/progress-centric-notifications>
- Apple HIG 通知管理：<https://developer.apple.com/design/human-interface-guidelines/managing-notifications> · WWDC21 时效性通知：<https://developer.apple.com/videos/play/wwdc2021/10091/>
- 鸿蒙 Notification Kit（渠道/授权，OpenHarmony API 参考）：<https://docs.openharmony.cn/>（`js-apis-notificationManager.md`，SlotType/SlotLevel/requestEnableNotification）
- 鸿蒙实况窗 Live View Kit：<https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/liveview-introduction> · 设计规范：<https://developer.huawei.com/consumer/cn/doc/harmonyos-guides/liveview-design-formula>
- Expo SDK 57 notifications：<https://docs.expo.dev/versions/v57.0.0/sdk/notifications/>

---

## 9. 后续立项清单

- [x] 待办一次性开始提醒、独立设备绑定与对账
- [x] expo-notifications 与版本化 reminders 渠道（sync 未创建）
- [x] 设置入口、保存后权限申请及拒绝引导
- [x] Android 16 动态通知：ProgressStyle 原生模块、设置页演示与待办进行中卡片（2026-09-23）
- [ ] Android 16 动态通知真机验收（API 36+：60 秒模拟待办、进行中卡片、退后台续算与撤卡、OEM 渲染）
- [ ] Android 真机权限、锁屏、杀进程、OEM 延迟与点击验收（用户主动发起）
- [ ] iOS macOS 构建/签名与设备验收；自定义 threadIdentifier 分组
- [ ] （远期）irisapi 服务端推送评估（部署手册 §19）
- [ ] （远期）鸿蒙端构建路线决策（react-native-harmony / 原生）
