# IRisNote 视觉设计规范（HyperOS 风格）

> 文档版本：1.20（2026-09-24：修改邮箱页，登录失效时编辑页不再拦截跳转；1.19 修改/重设密码页与登录失效横幅；1.18 地区选择页与关于页数据来源；1.17 性别弹窗取消「自定义」；1.16 个人资料编辑页、性别与离开确认弹窗、头像回退；1.15 头像更换迁移至个人资料页；1.14 新增「个人资料」规范；1.13 新增「主页面分页内容区与滑动接缝」规范）
> 本文档定义 IRisNote 的视觉规格（"长什么样"）；公共组件的形状、颜色、状态和接口契约见 [公共组件规范](./公共组件规范.md)；样式代码组织规则见 [样式开发规范](./样式开发规范.md)（"怎么写"）；完整 HyperOS 原始规格与色板见 [miuix设计参考（HyperOS风格）](./miuix设计参考（HyperOS风格）.md)。
> 修改任何视觉规格必须先更新本文档并记录 `CHANGELOG.md`，再动代码。

## 0. 真机布局实测验收（uiautomator 对账）

视觉规格的以下三个场景必须先做真机布局实测，再下结论：

- **截图判读**：判断截图中组件尺寸/间距是否合规，以实测数值为准，不凭目测；
- **提出修改要求**：需求须附实测数据（当前实际值 vs 规范值），不接受"看起来不对"式反馈；
- **验证改动**：改后在真机导出控件树核对 dp 值，配合截图交用户验收。

实测步骤（真机已连接 adb）：

```bash
node scripts/ui-dump-parse.mjs   # 一键导出+解析，输出每个元素的 px 与 dp（--serial 指定设备）
adb shell wm density             # 需手动换算时查密度（1dp = 密度/160 px）
```

判读规则：

- dump 坐标是 **px**，须除以 `密度/160` 换算 dp（例：476dpi → 1dp=2.975px）；
- 文本 bounds 是**文字边界不是触摸热区**（例：`min-h-11` 按钮热区 44dp，文字仅约 18dp）；
- 原生树经 RN 扁平化，**看不到组件名与 padding 来源**，定位原因必须回源码；
- 可交互性以 content-desc（对应 accessibilityLabel）与 clickable 计数交叉判断；
- clickable 节点数天然多于 RN 触摸组件数（TextInput 外层容器等也被标记），1:1 对不上属正常；
- **无真机/未连接 adb 时跳过本步骤**，结论退回静态检查与目测，不得编造实测数值。

## 0.1 主页面分页内容区与滑动接缝

- `SwipeTabs` 的分页表面固定为 `surface` 白色；`app-background`（`rgb(242, 242, 242)`）只允许出现在内容区顶部 15dp 留白与右侧 75dp 日历栏，不得透出为分页之间的竖线。
- 笔记与待办内容页的白色填充层必须与圆角边框层分离：外层负责 `surface` 白底及对应的 30dp 单侧顶圆角，内层负责 1dp `note-page-border` 和内容内边距；两层之间的 gap、margin、padding 均为 0dp。
- 笔记内层保留上、左、下边框；待办内层保留上、右、下边框。相邻分页的连接边不重复画边框。
- 内容区使用 `flex: 1` 连续铺满，不得以 `height: 100%` 配合底部 margin 人为留缝。横滑过程中的两页白色填充层必须连续覆盖分页边缘，不得露出灰色或黑色竖线。
- 静态检查只能确认分层与尺寸约束；改动后的无缝显示须在已连接真机上停在页面、滑动中、滑动结束三个状态分别验收。

## 1. 总则

- 风格基线：HyperOS / miuix 设计语言（分组卡片、squircle 观感的连续圆角、克制的单主色、分割线不贯通、弹层遮罩统一）。
- **主色决策（已定）**：沿用项目现有 `#007AFF`（iOS 蓝），不采用 miuix 的 `#3482FF`。主色切换属全局产品决策，另行立项（见 `docs/待办/TODO.md` §2）。
- 当前仅浅色模式；深色模式缺失是已知债务（`docs/待办/TODO.md` §2），本文档所有 Token 暂只有浅色值。
- 单一数据源：默认浅色主题原始值定义在 `src/shared/theme/presets/default-light.json`；TypeScript 主题对象直接解析该预设，`global.css` 托管区块通过 `npm run theme:sync` 生成，并用 `npm run theme:check` 检查一致性。
- 公共组件不得绑定当前色值，必须引用《公共组件规范》的语义 Token。后期更改主色、表面色、文字色或反馈色时，按完整主题预设替换，页面与组件接口保持不变。

## 2. 设计 Token（hyper-* 前缀）

| Token（global.css） | 镜像（colors/radius） | 值 | 出处 |
| --- | --- | --- | --- |
| `--color-hyper-card` | `hyperCard` | `#f0f0f0` | miuix secondaryContainer（浅色） |
| `--color-hyper-card-selected` | `hyperCardSelected` | `#eaf2ff` | miuix tertiaryContainer（选中行底） |
| `--color-hyper-divider` | `hyperDivider` | `#e0e0e0` | miuix dividerLine（浅色） |
| `--color-hyper-text-secondary` | `hyperTextSecondary` | `#8c93b0` | miuix onBackgroundVariant（蓝灰辅助文字） |
| `--color-hyper-error` | `hyperError` | `#e94634` | miuix error（浅色） |
| `--color-hyper-outline` | `hyperOutline` | `#d9d9d9` | miuix outline（未选中 radio 描边） |
| `--color-hyper-scrim` | —（仅 className） | `rgba(0,0,0,0.3)` | miuix windowDimming（浅色 30%） |
| `--color-hyper-primary-disabled` | `hyperPrimaryDisabled` | `#c2d9ff` | miuix disabledPrimary（primary 禁用底） |
| `--color-hyper-danger-disabled` | `hyperDangerDisabled` | `#f8d7d2` | danger 禁用底（error 30% 淡化推导） |
| `--color-hyper-secondary-disabled` | `hyperSecondaryDisabled` | `#f7f7f7` | secondary 禁用底（比卡片更浅一档） |
| `--color-hyper-label-disabled` | `hyperLabelDisabled` | `#b2b2b2` | 非强调禁用字色 |
| `--color-hyper-primary-faded` | `hyperPrimaryFaded` | `#80bfff` | 弱化（text）变体禁用字色 |
| `--radius-hyper-modal` | `hyperModal` | `24px` | 弹窗卡片圆角 |
| `--radius-hyper-card` | `hyperCard` | `16px` | 分组卡片圆角 |
| `--radius-hyper-control` | `radii.control` | `16px` | 普通按钮及迁移期 HyperOS 控件圆角 |
| `--radius-icon-control` | `radii.iconControl` | `16px` | 有底图标按钮圆角 |
| `--radius-field` | `radii.field` | `16px` | 输入框圆角 |
| `--radius-icon-cell` | `radii.iconCell` | `16px` | 图标选择单元圆角 |
| `--radius-progress-bubble` | `radii.progressBubble` | `16px` | 阅读进度气泡圆角 |
| `--radius-editor-toolbar` | `radii.editorToolbar` | `16px` | 无键盘浮动编辑器工具栏圆角 |

## 3. 字体

沿用 miuix 字号梯度，RN 中直接用 `text-[Npx]`：

| 层级 | 字号 | 字重 | 用途 |
| --- | --- | --- | --- |
| 弹窗标题 | 24（`text-2xl`） | Regular | 弹窗/小节标题，**不用粗体**做层级 |
| 列表主文字 | 17（`text-[17px]`） | Regular | 列表行标题 |
| 辅助正文 | 14（`text-sm`） | Regular | 说明、错误文案 |
| 脚注/元信息 | 13（`text-[13px]`） | Regular | 时间戳、提示性小字（`hyper-text-secondary`） |

原则：层级靠字号与颜色区分，禁止随意加 `font-bold`；例外仅限警示句中的关键词（如"丢弃本次修改"，用户指定）。

## 4. 弹窗规范（已落地：草稿弹窗族）

结构（自上而下）：

```text
遮罩  bg-hyper-scrim（30% 黑），内容居中，外边距 24；点遮罩空白处关闭（closeOnScrimTap=false 可禁用，用于二次确认）
卡片  白底 · rounded-hyper-modal(24) · p-6 · max-w-[440px] · max-h-[85%]；卡片自身拦截触摸不冒泡到遮罩
标题行 mb-3 · flex-row justify-between：标题 text-xl · Regular · 黑色，左侧可选 leading 标识图标（24dp、textPrimary，如草稿箱 Inbox，与分类标题图标同规格，静态不可点）；右侧可选 headerExtra（如图标按钮，28x28 触控区）
内容  （选择列表 / 文案 / 状态，见 §5–§7）
提示  单行图标行（公共 InlineHint 默认 compact）：CloudOff 14dp + 6dp +「草稿仅本机保存，不会同步到云端。」；文本 13sp、18dp 行高、灰蓝 textSecondary、尾部省略。未传 onPress 时为静态内容；组件不带外边距，提示行到按钮行由调用方留 12dp（页脚 mt-3，浏览/删除两模式统一）
重要提示  使用 InlineHint `important`：如 Trash2 14dp +「删除草稿不可恢复」，图标与文本均为 destructive 红色；无背景、无圆角，文本固定单行尾部省略。传入 onPress 时才显示按压反馈；次行补充说明不挂图标、不缩进
按钮  见 §6 容器规则（1 个通栏 / 2 个左右等分 / 3 个两容器 / 多个纵向列表）
```

- 必须使用 `AppModal`（`@/shared/ui/Overlay/app-modal`）承载——全局通知插槽依赖它，禁止直接用 RN `Modal`。
- 动画：`animationType="fade"`；关闭回调透传 `onRequestClose`。

## 5. 选择列表规范（DraftChoices 模式）

```text
滚动视口  wrapper：bg-hyper-card · rounded-hyper-card(16) · overflow-hidden（圆角恒在，内容超限时在圆角内滚动）
          内层 ScrollView：style maxHeight 300dp ≈ 3.7 行（每行约 80dp），露出部分第 4 行提示下方还有内容
行        min-h-[56px] · flex-row · items-center · gap-3 · px-4 py-3（行间无分割线）
  主文字  text-[17px] · numberOfLines=1
  元信息  text-[13px] hyper-text-secondary · mt-1 · numberOfLines=1

浏览态（单选）：选中行 = 浅蓝底 bg-hyper-card-selected + 主文字与副文本变 text-primary + 行尾 Check 图标 20（主色）
删除态（多选）：不加底色；勾选行 = 主文字与副文本变 text-hyper-error + 行尾 Check 图标 20（hyperError）
入口：标题行右侧 Trash2 图标（20 号，hyperTextSecondary，28x28 触控区），列表非空且浏览态才显示
```

> 实现注意：ScrollView 的 max-height 必须写原生 `style={{ maxHeight: ... }}`，NativeWind 的 `max-h-[Npx]` 任意值类对 ScrollView 自身尺寸实测不编译生效（2026-09-10 真机证据）。

## 6. 弹窗按钮规范（DialogButton）

单一组件 `DialogButton`（`features/notes/components/editor/draft-dialog.tsx`，带 `label` 字符串属性），禁止字符串 children。
组件提升时机：当前位于 notes feature 内；当第三个 Feature（分类弹窗、编辑页对话框等）采用时，按《样式开发规范》的提升规则上移 `src/shared/ui`。

### 6.1 层级与配色（含字体）

| 层级 | 变体 | 背景 | 字色 | 禁用底色 | 禁用字色 |
| --- | --- | --- | --- | --- | --- |
| 强调 · 下一步操作 | `primary` | 品牌蓝 `#007AFF` | 白 | `#C2D9FF` 浅蓝（`hyper-primary-disabled`） | 白 |
| 强调 · 错误 | `danger` | 错误红 `#E94634` | 白 | `#F8D7D2` 浅红（`hyper-danger-disabled`） | 错误红 `#E94634`（用户已确认） |
| 非强调 | `secondary` | 浅灰 `#F0F0F0`（hyper-card） | 黑 | `#F7F7F7`（`hyper-secondary-disabled`） | `#B2B2B2`（`hyper-label-disabled`） |
| 弱化 · 带底 | `tonal` | 浅灰 `#F0F0F0`（hyper-card） | 品牌蓝 `#007AFF` | `#F7F7F7`（`hyper-secondary-disabled`） | `#80BFFF`（`hyper-primary-faded`） |
| 弱化 · 无底 | `text` | 透明 | 品牌蓝 `#007AFF` | 透明 | `#80BFFF`（`hyper-primary-faded`） |

**字体（全层级统一）**：17px（miuix button 档）· Regular 字重（HyperOS 不以粗体做强调）· 单行截断（`numberOfLines=1`）· 水平垂直居中；不设显式行高。

**交互态**：

- 按压：整体 `opacity 0.85`，松开恢复（全变体统一）。
- 禁用：原生 `disabled` 属性 + 上表"禁用底色/字色"显式 token（不用透明度叠加，颜色不受背景影响）；禁用即拦截点击。

- `className` 透传：DialogButton 接受 className 做布局扩展（如 flex-1），语义颜色仍只能通过 variant 切换。
- 无障碍：`accessibilityRole="button"` + `accessibilityLabel={label}` + `accessibilityState.disabled`（浏览器回归按可访问名称点击，改名需同步测试）。

### 6.2 danger 使用边界

danger（红色强调）**只用于不可逆操作的最终确认步**——即二次确认框内的执行按钮（如草稿删除确认的「删除」）。入口层的破坏性动作（如离开确认框的「不保存」）一律用 secondary 浅灰，把红色留给最后一步。

### 6.3 按钮容器布局规则

按弹窗内**交互按钮数量**决定容器结构（容器 = 一行按钮的包裹 View）：

| 按钮数 | 容器数 | 布局 |
| --- | --- | --- |
| 1 | 1 个容器 | 按钮独占一行（通栏） |
| 2 | 1 个容器 | 两按钮左右排列，等分宽度（flex-row · gap-2.5 · 各 flex-1） |
| 3 | 2 个容器 | 第 1 容器：两按钮左右排列；第 2 容器：位于第 1 容器下方，按钮独占一行（通栏） |
| 4 个及以上 | 按钮列表 | 每个按钮独占一行，纵向排列 |

- 容器之间纵向间距 `gap-2.5`；同容器内左右按钮 `gap-2.5`。
- 每个容器内的按钮高度仍按变体规格（h-12 / h-11）。

### 6.4 按钮归属原则

- **互斥的两个操作固定同容器**：确认/取消、进入编辑/关闭、保存草稿/不保存、退出删除/确认删除这类"二选一"操作，放同一容器内左右两按钮。
- 3 个及以上按钮时：先识别互斥对放入第 1 容器（左右排列），其余操作归入第 2 容器（或 4+ 时的列表）。
- **归属有歧义时必须询问用户**（哪个操作与哪个互斥、第 2 容器放什么），不得自行决定。
- 现有实例对照：离开确认弹窗 = 第 1 容器「保存草稿 / 不保存」（互斥）+ 第 2 容器「继续编辑」（通栏）。

## 7. 状态规范

| 状态 | 规格 |
| --- | --- |
| 加载 | `ActivityIndicator`，`color={colors.primary}`，上下留白 my-8 |
| 空状态 | 居中 `Inbox` 图标 28（hyperTextSecondary）+ `text-sm` 蓝灰文案，如"暂无草稿" |
| 错误 | `text-sm text-hyper-error` + `accessibilityRole="alert"`；重试用 `text` 变体按钮 |

## 新建分类表单与图标选择

- 新建分类采用 §4 弹窗外壳：24dp 内边距与圆角、最大宽 440dp、可用区域最大高度 85%；标题左对齐、24sp 常规字重。键盘展开或高度不足时表单内容滚动，底部按钮固定在卡片内。
- 分类名称标签 14sp 蓝灰，与输入框间隔 8dp；输入框高 48dp、浅灰 hyper-card 底、16dp 圆角、2dp 常驻透明边框、左右内边距 16dp、文字 17sp；聚焦为主题蓝 2dp 边框，错误为红色 2dp 边框。
- 输入框到图标展开行间隔 16dp；展开行高 48dp、宽度铺满内容区，整行可点击。「选择图标」与 24dp 深灰 Lucide 图标在左侧相邻排列，间距 4dp。每次打开默认折叠，使用 ChevronRight；展开使用 ChevronDown；收起保留已选图标。
- 展开行到面板间隔 8dp；图标面板最高 240dp，按分组纵向滚动；分组标题 13sp 蓝灰，与图标间隔 8dp；组间距 12dp。图标块 48×48dp、圆角 16dp、图标 24dp，以 8dp 间距自动换行，取消组内横向滚动。未选中浅灰底灰图标，选中浅蓝 hyper-card-selected 底、主题蓝图标与 1dp 蓝色边框。
- 面板或收起行到按钮间隔 16dp；取消在左、确定在右，等宽、高 48dp、间隔 10dp，采用 DialogButton secondary/primary 及其禁用样式。

## 分类操作与删除确认

- 分类操作沿用 24dp 圆角与内边距、最大宽 440dp、最大高度 85% 的弹窗外壳。标题行高 48dp：行首为当前分类图标按钮 48×48dp（内含 24dp 图标、textPrimary 色，点击切换图标面板展开/收起），分类名称不加装饰引号、24sp 常规字重、垂直居中，右侧重命名编辑触控区 48×48dp；编辑态整行被改名输入框替换，图标按钮不显示。改名输入框高 48dp，浅灰底、16dp 圆角、2dp 常驻透明边框、17sp 文字，保留失焦与键盘提交方式。
- 标题到状态控件间距 12dp；置顶/标星等宽，高 48dp、圆角 16dp、间距 10dp、文字 17sp。未启用浅灰底，启用浅蓝底、主题蓝文字和图标。
- 「更改图标」行已删除：图标面板由标题行分类图标按钮切换，展开时位于标题行之下、置顶/标星之前（面板与状态行间 8dp，mb-2；收起时不占位），复用最高 240dp 的分组纵向网格；选完图标随父级状态更新自动收起。
- 图标区到删除入口间隔 16dp。删除入口用 secondary 浅灰底黑字，高 48dp；保留右滑到垃圾桶再二次确认的流程，删除引导用浅灰卡片和蓝灰文案。取消按钮高 48dp，距引导卡片 12dp。
- 最终确认弹窗改用共享 DeleteConfirmDialog（见「删除确认弹窗」节）：标题 24sp，提示 14sp 蓝灰两行，提示到按钮 12dp。取消在左、红色确认删除在右，等宽、高 48dp、间距 10dp；危险强调仅用于最终执行按钮。确认后进入 2 秒倒计时再执行。

## 笔记操作窗口

- AppModal 外壳：30% 遮罩、24dp 外边距与内边距、24dp 圆角、最大宽 440dp、最大高 85%；内容超高滚动。
- 左上角「笔记操作」24px 常规字重。标题行：标题文字在行首占满（flex-1，最多两行），编辑图标触控区 44dp 在行尾；图标仅改标题，编辑态使用公共 `InputSave`。输入框与右侧保存按钮均高 52dp、间距 8dp、连续圆角 16dp；输入框为浅灰 `surfaceControl` 底、2dp 常驻透明边框，聚焦显示主题蓝 2dp 边框，错误显示红色 2dp 边框，左右内边距 16dp、文字 17sp。保存按钮为浅灰底，使用 Lucide `Save` 20dp、`textSecondary` 灰色；保存中原位锁定并显示灰色指示器。点击保存与失焦、键盘完成、点击弹窗外部或系统返回均复用自动保存，本地先保存再同步，不再显示「放弃修改」「保存标题」文字按钮。改名成功一律静默（编辑框收起、标题即时更新），失败错误红字显示在提示行；标题为空时保留编辑态并阻止关闭。
- 结构顺序与间距节奏（对齐退出笔记弹窗）：标题行下隔 16dp 为信息区，信息区在置顶/标星之前；信息区内部两行行距 12dp；信息区到置顶/标星 12dp；置顶/标星到操作条 12dp。
- 信息区第一行：云同步使用公共 `InlineHint` 的 `standard` 可点击模式（云图标 20dp 与状态文字间隔 4dp，图标+文本整体可点），与文件大小之间固定 20dp，1dp × 20dp 浅灰竖线在 20dp 内垂直居中。状态为已同步/等待中/未同步/云同步已关闭，上传时显示同步中；点击触发一次上传，保留未知创建防重复保护。上下触控热区各 12dp；同步中、繁忙或无账号时禁用点击。未接入独立云同步开关，关闭态仅对应无账号情况。
- 同步反馈三态原地显示，不单独成行：成功一律静默（仅图标状态流转 ☁↑同步中… → ☁✓已同步）；失败时按钮内文字替换为错误文案，图标与文字同用错误红 `text-hyper-error`；重试清错恢复「同步中…」。
- 文件大小按标题与正文 UTF-8 字节数计算，不含外部附件；1024 进制，超过 1024MB 显示 GB，两位小数，非空且不足 0.01MB 显示 <0.01MB。
- 信息区第二行：Clock、预计阅读时间、上次阅读百分比；CircleAlert 14dp 浅灰紧跟文字后 8dp，与文字行中线垂直居中（兄弟节点 + items-center，不得嵌入 Text 内基线对齐）；整行打开现有字数统计模块。阅读进度按滚动位置估算、分账号和笔记本地记录；未记录显示尚未阅读。

## 笔记查看与编辑合并页

- 顶栏总高保持 64dp，左侧为公共 `BackButton` 的 40dp Lucide 返回图标触控区（布局外 hitSlop 2dp），右侧保留 40dp 空位使中间标题严格居中；不提供右上角进入编辑入口。正文滚动容器左右内边距 20dp、顶部 18dp，标题与元信息间距 12dp，元信息与正文间距 24dp；无键盘时底部浮动工具栏为 225×48dp、`radii.editorToolbar`（16dp）连续圆角矩形，距底部 40dp；键盘打开后切换为 43.2dp 通栏工具栏并紧贴输入法，不保留额外间距，通栏状态圆角为 0dp。
- 标题与正文始终使用与阅读排版一致的可编辑文本节点。单击在触点插入主题色光标但不弹出软键盘；同一输入区 300ms 内第二次点击保留光标位置并调起输入法。读屏开启时遵循系统焦点与激活动作，不要求用户执行自定义双击。
- 输入期间继续按 750ms 防抖、5 秒最长等待写入本地恢复草稿；系统返回或顶部返回收起输入法后执行本地优先正式保存，保存完成仍停留当前笔记页。无变化不创建版本、不重复上传；保存失败保留草稿并使用全局重要通知。
- 既有笔记的普通本地草稿直接载入，不显示「发现本地草稿」整页提示。基础版本冲突时只显示紧凑错误提示与「使用本地草稿」确认入口，未经确认不得自动覆盖当前稳定版本；新建笔记草稿箱及其恢复、删除流程不受影响。
- 置顶/标星等宽、高 48dp、间距 10dp，选中浅蓝底、主题蓝文字图标。下隔 12dp 为编辑 | 复制 | 分享 | 删除容器，四项等宽、图标在上文字在下，触控高度至少 48dp。分隔线 1dp × 20dp，两侧内容各留 10dp。删除走共享删除确认弹窗；主窗口无取消按钮，通过遮罩或系统返回关闭。
- 分享格式、图片设置保留原功能与返回入口，图片捕获节点继续独立挂载。
- 行高与触控约定：小行保持内容高度（图标 20dp + 文字），禁止用 min-h 撑高后垂直居中（居中留白使视觉间距失真）；触控区不足约 44dp 时用 hitSlop 上下外扩 12dp 补偿，不占布局。
- 弹窗内兄弟组件 key 带语义前缀（如 `rename-`/`info-`）＋ `${visible}:${id}` 结构：防 key 冲突，并在弹窗重开或切换对象时强制重挂载、重置子组件内部状态。

## 删除确认弹窗（DeleteConfirmDialog）

- 笔记删除与分类删除共用共享组件 DeleteConfirmDialog，两处像素级一致；DraftDialog 外壳，标题「确认删除？」24sp。
- 警示正文 14sp 蓝灰两行：「删除后无法找回」＋「笔记“X”将被永久删除」或「分类“X”下的笔记将被永久删除」；提示行到按钮行 12dp。
- 按钮行：取消（secondary 浅灰）在左、确认删除（danger 错误红）在右，等宽、高 48dp、间距 10dp；红色仅用于不可逆删除的最终执行步。
- 2 秒倒计时（与草稿删除统一）：首按「确认删除」→ 按钮变「删除中…」＋白色小 spinner，2 秒后执行并关闭；倒计时中再按按钮、点遮罩或返回键仅打断倒计时不关弹窗；删除执行中遮罩与返回不响应。
- 失败反馈：错误以 `text-sm text-hyper-error` ＋ `accessibilityRole="alert"` 红字显示在弹窗内（含登录不可用与接口错误的具体原因），不使用系统 Alert。数据流：入口仅打开弹窗，删除逻辑收进异步 onConfirm，失败抛错由弹窗展示。

## 阅读进度气泡（ProgressBubble）

- 跟随手指的坐标浮层（不走 AppModal）：拖动快速滚动条时出现在手指**左上方**，锚点为气泡外框右下角，距手指横向 50dp、纵向 100dp；越界时在正文容器安全边距 8dp 内自动避让。
- 外观：白底 `colors.surface` · 圆角 16px（progress-bubble 档）· `borderCurve: "continuous"` · 边框 1px `hyperDivider` · 阴影 `0 2px 8px rgba(0,0,0,0.12)`。
- 内边距水平 12dp、垂直 8dp；宽度随内容自适应，上限 **200px**（窄屏取容器宽 −16dp），下限 44px。
- 文字：标题与书签名 14px 黑色（textPrimary），最多**两行**换行截断；书签行 Bookmark 图标 14dp 主色，图标与文字间距 8dp，行与行间距 4dp。
- 无标题且无书签时：居中显示进度 `36.28%`（保留两位小数），14px 黑色。
- 生命周期：按住／拖动期间常显；松开快速滚动条后 **1 秒**消失（正文滚动不重置该计时）；快速滚动条本体维持停止交互 5 秒隐藏。交互规格见 [笔记查看进度组件](./笔记查看进度组件.md)。

## 欢迎、登录与注册页面

- 只参考用户提供图片的布局与排版，沿用 IRisNote 内容、图标和现有配色。
- 欢迎页白底，品牌组在按钮上方的可用区域居中：现有图标 80×80dp，间隔 24dp 为 32sp 品牌名，再隔 12dp 为 16sp 说明。底部注册/登录按钮纵向排列，间距 10dp、左右边距至少 44dp、最大宽 440dp、高 48dp，分别使用 primary/tonal，距底部安全区 24dp。短屏整体可滚动。
- 登录注册白底单列，内容最大宽 440dp，宽屏水平居中；左右外边距至少 16dp，顶部距安全区 12dp，返回触控区 44×44dp，底部留白 24dp。表单随内容滚动并避让键盘。
- 返回栏下隔 8dp 显示居中的 24sp 黑色常规字重标题；再隔 12dp 显示居中的 14sp 蓝灰说明；标题区到表单 32dp。品牌图标与名称放在欢迎页。
- 登录字段顺序：邮箱、邮箱验证码、密码。注册字段顺序：用户名、邮箱、邮箱验证码、密码、确认密码。字段组间隔 28dp。
- 标签 14sp，距输入框 8dp；输入框高 48dp、16dp 圆角、2dp 常驻透明边框、左右内边距 16dp、正文 17sp、hyper-card 浅灰底。聚焦时 2dp 蓝边，错误时 2dp 红边，错误文字位于框下 6dp、14sp。密码图标 20dp，触控区 48×48dp。
- 验证码输入与发送按钮间隔 10dp；发送按钮宽 120dp、高 48dp、圆角 16dp、浅灰底蓝字。发送中原位显示加载状态，成功后 60 秒倒计时。
- 主按钮距表单 28dp，通栏、高 48dp、16dp 圆角、品牌蓝底白字，17sp 常规字重；使用显式 HyperOS 禁用 Token，按压透明度 0.85。
- 底部切换入口距主按钮 16dp，居中，提示与蓝色链接相隔 4dp，链接触控高度至少 44dp。
- 初始空表单不显示错误；字段失焦后展示校验错误；提交中锁定表单与发送、切换入口，阻止重复提交。接口失败在主按钮上方显示红色错误，距按钮 12dp，保留输入；验证码发送成功使用全局横幅；认证成功保存并刷新会话后直接进入用户页，展示成功横幅。
- 未登录进入主应用时先到 /auth/welcome；欢迎页进入登录/注册。两表单的返回按钮及 Android 返回键回欢迎页，提交/发送中拦截返回；账号切换链接替换当前表单路由，避免互相堆叠。直接打开表单时，返回按钮仍可进入欢迎页。
- 两页复用 features/auth/components 内的 AuthScreenLayout、AuthField、AuthButton，欢迎页复用 AuthButton，使用现有 Theme Token。

## 用户中心

- 用户中心定位为移动端个人内容工作台，不重复承担设置页职责。页面移除顶栏与整块蓝色资料头图，从安全区下方 16dp 直接进入账户卡；底色使用 `app-background`，页面根容器和滚动内容同时占满手机可用宽度与高度，不设桌面宽屏最大宽度。
- 内容区四周内边距统一为 16dp，不为右侧悬浮 Tab 栏预留横向或纵向空间。内容不足一屏时按顶部、中部、底部三区弹性分布：顶部为账户卡与内容概览，中部为继续阅读，底部为我的内容；区块最小间距 20dp。内容超过一屏时自然滚动。
- 账户卡白底、16dp 圆角、16dp 内边距。头像固定 64×64dp，头像到文字 14dp，昵称固定 20sp。邮箱和加入时间为 13sp 蓝灰，三行均单行截断；整张账户卡（含头像）为一个触控区，进入个人资料页，右侧 18dp 箭头距文字 8dp；用户中心不再提供更换头像入口与上传遮罩。不为 320dp 等超小视口缩小头像、字号或内容宽度。
- 账户卡下隔 16dp 放三格内容概览卡。三项依次为笔记、分类和标星；卡片白底、16dp 圆角、水平内边距 8dp、垂直内边距 12dp，图标 22dp、数字 17sp、标签 13sp，项目间用上下缩进 12dp 的 1dp 分隔线。加载时数字位置显示指示器，来源不可用显示“—”，禁止演示数字。
- 概览下隔 20dp 为“继续阅读”。分组标题 13sp 蓝灰、距卡片 8dp；有记录时卡片内边距 16dp，首行使用 22dp 图标、17sp 单行标题和 18dp 箭头，下隔 16dp 放 6dp 高进度条，再隔 8dp 显示 13sp 百分比。只显示当前账号最新、仍未读完且笔记仍存在的结构化阅读记录。
- 没有可继续记录时使用同尺寸白色空状态卡：26dp 阅读图标、下隔 12dp 的 14sp 说明、下隔 4dp 的 13sp 蓝色“去看看笔记”；点击进入全部笔记。
- “我的内容”与前一区块间距 20dp，包含全部笔记、星标笔记、草稿箱三行。每行最小高 56dp、左右内边距 16dp、垂直内边距 12dp，图标 22dp、图文间距 12dp、标题 17sp、数量 13sp 蓝灰、箭头 18dp，分割线左右缩进 16dp。
- 用户 Tab 悬浮主按钮使用设置齿轮并进入设置页；不在页面正文重复设置或退出登录入口。当前阶段明确忽略悬浮 Tab 栏对用户页内容的遮挡，Tab 布局优化需另确立项，不得再次压缩用户页内容规避。
- 用户页验收只按 Android/iOS 移动端口径执行，不以 Web 宽屏、桌面居中或 320dp 超小视口作为设计与视觉验收标准。基准竖屏尺寸为 390×844 与 412×915；浏览器宽屏表现不影响用户页验收结论。
- 页面支持纵向滚动；按压态透明度 0.85。按钮、头像、概览均提供可访问名称。原“深色模式、隐私设置、意见反馈”静态行全部移除，未接入能力不得显示成可点击入口。

## 设置首页

- 设置页只参考用户提供图片的纵向信息结构，不复制其业务内容；会员中心、购买入口、容量套餐、容量进度条和轮播圆点不进入 IRisNote。
- 页面使用浅色 HyperOS 分组结构：页面底色 `app-background`，品牌主色继续使用 `#007AFF`。内容区左右内边距 16dp、最大宽 560dp并水平居中；短屏允许纵向滚动，底部保留安全区加 32dp 空白。
- 顶栏高 64dp：左侧返回触控区 44×44dp，中间标题「设置」24sp 常规字重，右侧保留 44dp 空位以维持标题严格居中。无可返回历史时回到用户中心。
- 隔 16dp 放四格状态概览卡。概览卡为白底、16dp 圆角、上下内边距 12dp；四项等宽，图标 22dp，图标到标题 8dp，标题 14sp，状态值 13sp 蓝灰。首版依次显示外观「浅色」、阅读「默认」、通知「站内」、同步「按需」；概览只陈述当前状态，不伪装成可点击入口。
- 分组与前一区块间距 20dp；分组标题位于卡片外，13sp 蓝灰，距卡片 8dp。分组卡片为白底、16dp 圆角、内容裁切。
- 设置行最小高度 56dp，水平内边距 16dp、垂直内边距 16dp，图标 22dp，图标到文字间距 12dp。主文字 17sp，说明 14sp、行高 20dp，右侧状态 13sp；分割线 1dp，左右缩进 16dp。
- 首版分组顺序：偏好设置（外观与主题、编辑与阅读、通知设置）、数据与隐私（同步与备份、数据与存储、隐私与安全）、支持（帮助与反馈、关于 IRisNote）。尚无真实设置模型或目标页面的项目显示「规划中」、灰色禁用且无箭头；关于项只读显示应用版本。
- 退出登录位于所有分组之后 16dp，白底、16dp 圆角、高 48dp，使用错误红文字和图标但不使用红色实底；点击复用现有退出登录能力并返回欢迎页。未登录时不显示退出入口。
- 可点击项使用按压透明度 0.85，并提供 `accessibilityRole`、可访问名称和禁用状态。页面不新增设置持久化、后端请求、主题切换、系统通知或全局同步能力；这些能力必须后续独立立项。

## 同步暂存列表

- 页面只由“无法连接服务器”全局横幅的“查看暂存”动作或上传暂停横幅正文进入，不在设置页增加入口。
- 页面获得焦点后立即收回“无法连接服务器”横幅，避免遮挡顶栏和概览；页面聚焦期间该横幅不重新出现。离开页面后若服务器仍不可用则恢复显示，服务器已恢复则保持收回。上传进度、暂停和任务阻断等其他横幅不受影响。
- 顶栏高 64dp：左侧返回触控区 44×44dp，中间标题“同步队列”24sp，右侧 44dp 空位。页面底色使用 `app-background`。
- 内容左右边距 16dp。顶栏下方概览卡为白底、16dp 圆角、16dp 内边距；三行依次展示暂存任务数、预计上传量和当前网络类型，图标 20dp、图标到文字 10dp、行间距 8dp。
- 概览卡到任务列表间距 20dp。任务列表白底、16dp 圆角，按内容自动增高，超出页面剩余高度后才在圆角范围内滚动，不用空白区域撑满页面。每行最小高度 72dp、内边距 16dp；文字字段间距 4dp，文字区到删除组件 12dp。标题 16sp 半粗，操作/预计流量/状态为 13sp，尝试次数、最近错误与禁用原因 12sp；行间分割线 1dp、左右缩进 16dp。
- 行尾删除入口复用公共 `IconButton compact`：组件 40×40dp、触控区 44×44dp、垃圾桶图标 20dp，默认中性色。`running` 任务和无上一 Revision 的笔记任务禁用。
- 删除笔记任务打开 280dp 宽“确认回滚？”弹窗：面板内边距 20dp，标题到正文 12dp，双按钮间距 10dp，按钮高 48dp；左侧“取消”为次要层级，右侧“确认回滚”为危险实底。执行时显示“回滚中…”和旋转指示器，锁定按钮与遮罩关闭；失败文案在弹窗内使用错误色。已经尝试上传时额外提示云端状态可能需要核对。
- 空列表在卡片内使用 24dp 内边距居中显示“暂无暂存任务”。读取失败显示错误红文案及最小高度 48dp 的“重新读取”动作。
- 预计上传量仅表示 UTF-8 JSON 请求体，不包含协议和响应开销；不得把估算值标成运营商实际计费流量。

## 个人资料

> 状态：规范已定，尚未实现。实施分阶段见 [个人资料页面规划与实施计划](../进度与验证/个人资料页面规划与实施计划.md) 第 9 节。尺寸以本节为准，规划文档中与本节不一致的早期数值作废。

- **入口**：用户中心账户卡整卡进入个人资料页，账户卡尺寸不变，右侧增加 18dp 箭头，与文字区间隔 8dp；设置页标题下 16dp 新增「账户」分组，含一行「个人资料」（说明“头像、用户名与账户信息”，带箭头），原状态概览卡顺移至该分组之后 20dp。
- **页面壳**：底色 `app-background`；内容左右 16dp、最大宽 560dp 水平居中；顶栏 64dp，左侧返回触控区 44×44dp，中间标题 24sp 常规字重，右侧 44dp 空位。标题栏到首卡 16dp，底部留白为底部安全区 + 32dp，仅由页面消费一次；根布局负责顶部安全区。
- **顶栏与资料行**：与设置页共用同一组公共页头与列表行组件（实施时由设置功能目录提升到 `src/shared/ui`，设置页同步改引用，外观不变）。
- **头像卡**：白底、16dp 圆角、16dp 内边距，整卡为一个触控区，点击以头像为锚点打开头像操作菜单；头像 64×64dp，右下角相机角标 22×22dp（主色底、2dp 白边、12dp 白色图标，向外偏移 2dp），头像到文字 14dp；昵称 20sp、邮箱 13sp 蓝灰，均单行截断；右侧「更换头像」13sp 蓝灰与 18dp 箭头间隔 8dp，距文字区 8dp。选图或上传进行中整卡禁用。
- **分组**：依次为「基本资料」（用户名、性别、地区、个人简介）、「账户与安全」（邮箱、修改密码、平台绑定）、「账户信息」（用户 ID、注册时间）。分组间 20dp；分组标题 13sp 蓝灰，距卡片 8dp；卡片白底、16dp 圆角、内容裁切。
- **资料行**：最小高 56dp，水平与垂直内边距 16dp，图标 22dp，图标到文字 12dp；主文字 17sp，右侧值 13sp 蓝灰，箭头 18dp；分割线 1dp、左右缩进 16dp。简介行最多两行预览。只读行无箭头；用户 ID 行尾复制按钮触控区 44×44dp。邮箱脱敏显示。
- **未开放项**：尚无后端能力的行沿用设置页约定，显示「规划中」、灰色禁用、无箭头，不得显示为可点击（性别、地区、个人简介、修改密码均已开放）；邮箱行显示脱敏邮箱并带箭头进入修改邮箱页；平台绑定显示「敬请期待」并进入占位说明页。
- **编辑页**：沿用本页壳；字段说明 14sp，标签到输入框 8dp；输入框使用公共 `Input` 标准尺寸 48dp（多行正文尺寸按组件），错误/计数行距输入框 8dp（左说明或错误、右“已输入/上限” 13sp）；表单项间 16dp；表单到通栏主按钮 24dp，主按钮使用 `AppButton` 标准 48dp。键盘弹起时整页滚动保证输入与按钮可见。
- **用户名与简介编辑页（已实现）**：字段放在白卡内（16dp 圆角与内边距）：标签 14sp 蓝灰，下隔 8dp 为输入框（用户名标准 48dp，简介为 144dp 多行），再隔 8dp 为左侧说明 13sp 蓝灰（出错时换为 14sp 红字）与右侧“已输入/上限” 13sp；简介标签行右侧有“清空”14sp 主色文字，触控上下扩展 12dp，仅有内容时显示。卡片下 24dp 为通栏保存按钮（有保存错误时，错误红字距卡片 12dp、按钮距错误 12dp）。保存成功返回个人资料并显示“已保存”横幅；有改动时离开弹出“放弃修改？”确认，保存中阻止离开。
- **性别弹窗（已实现）**：§4 外壳，标题「性别」；§5 选择列表（不设置/男/女，行 ≥56dp，选中浅蓝底、主色字与 20dp 勾，行间无分割线）；列表下 12dp 为按钮行，保存失败时列表下 12dp 为错误红字、红字与按钮间 12dp；按钮行「取消 / 保存」等分、间距 10dp、高 48dp。未改变选择时保存禁用；保存中显示「保存中…」并锁定遮罩与返回；平时点遮罩等同取消。
- **地区选择页（已实现）**：标准页壳，内容区与底栏最大宽 560dp。顶栏下 16dp 为 48dp 搜索框（左侧 20dp 搜索图标，可一键清空）；进入某国后下隔 12dp 为面包屑行（最小高 44dp：「全部」14sp 主色可点，14dp 箭头，当前国家 14sp 黑色）；已保存地区在字典中失效时，下隔 12dp 显示 14sp 蓝灰“原地区「{名称快照}」已调整，请重新选择”。其下 12dp 为占满剩余高度的白色列表卡（16dp 圆角、内容裁切、卡内滚动），行 ≥56dp、px16 py12，分割线 1dp 左右缩进 16dp；顶层首行「不设置」，进入国家后首行「选择「{国家}」，不再细分」；有下级的国家行尾 18dp 箭头，选中行浅蓝底、主色字与 20dp 勾；搜索时为跨层级扁平结果，省州行下方 13sp 蓝灰显示所属国家，无结果显示居中 14sp“没有找到匹配的地区”。固定底栏：左右 16dp、上 12dp、下为底部安全区 + 16dp，“已选：{名称}”13sp 蓝灰单行，8dp 后为 48dp 通栏保存按钮（保存错误红字位于两者之间，上下各 8dp）。无改动时保存禁用，有改动离开时弹出「放弃修改？」。
- **关于页数据来源（已实现）**：品牌区之后、「版本记录」之前为「数据来源」分组（标题 13sp 蓝灰距卡片 8dp，卡片与「版本记录」标题间 20dp），只读列表行「地区数据」，说明 14sp 为数据集名称、ODbL v1.0 与“已按国内表述规范调整部分名称”，不可点击。
- **修改密码页（已实现）**：标准页壳，标题「修改密码」，忘记当前密码时切换为「重设密码」（同一页，切换时清空全部输入）。云存储关闭时顶栏下 16dp 为 `InlineHint`（云朵图标）“修改密码需要开启云存储，请先在「同步与备份」中开启”，卡片距提示 12dp，所有提交与发送按钮禁用；否则卡片距顶栏 16dp。字段沿用登录页 `AuthField`（标签 14sp 蓝灰距输入框 8dp，输入框 48dp，密码显隐 20dp 图标；错误 14sp 红字距输入框 6dp，失焦后显示），放在白卡内（16dp 圆角与内边距），字段间 16dp。修改模式：当前密码下 8dp 为右对齐“忘记当前密码？”14sp 主色（触控高 44dp）。重设模式：卡片首行 14sp 蓝灰“验证码将发送到 {邮箱脱敏}”，8dp 后为验证码输入与 120dp tonal 发送按钮（间隔 10dp，倒计时显示“Ns”，被限流时以服务端等待时间倒计时），其下 8dp 为右对齐“改用当前密码”。新密码下 6dp 为 13sp 蓝灰“6–64 个字符，不能与当前密码相同”（重设模式无后半句），出错时由字段错误替代；确认新密码不一致时显示“两次输入不一致”。卡片下 12dp 为 13sp 蓝灰“修改后本机保持登录，其他设备需要重新登录。”，提交错误红字再隔 12dp，其后 24dp 为 48dp 通栏主按钮「确认修改 / 重设密码」（提交中“修改中…”），条件不满足时禁用。成功显示“密码已修改/密码已重设”横幅并返回；有输入时离开弹出「放弃修改？」，提交中阻止离开。
- **修改邮箱页（已实现）**：标准页壳，标题「修改邮箱」。云存储关闭时顶栏下 16dp 为 `InlineHint`“修改邮箱需要开启云存储，请先在「同步与备份」中开启”，按钮禁用；步骤文字“第 N 步，共 2 步”13sp 蓝灰距顶栏 16dp（有提示时距提示 12dp），下隔 8dp 为白卡（16dp 圆角与内边距）。第 1 步：卡内“验证身份”17sp，4dp 后 13sp 蓝灰说明（验证码方式“验证码将发送到 {邮箱脱敏}”，密码方式“输入当前账号密码以确认是您本人”），16dp 后为验证码字段（输入与 120dp tonal 发送按钮间隔 10dp）或当前密码字段，其下 8dp 为右对齐“改用密码验证 / 改用邮箱验证码”14sp 主色（触控高 44dp，切换时清空输入）；主按钮「下一步」（验证中…）。第 2 步：新邮箱字段（格式错误失焦后显示），16dp 后为验证码字段（新邮箱格式有效才可发送；修改新邮箱后验证码清空、倒计时重置，需重新发送），8dp 后为 13sp 蓝灰“修改后请用新邮箱登录，已登录的设备不受影响。”；主按钮「确认修改」（修改中…），已向当前填写的新邮箱发送验证码并填写后可用。卡片下 12dp 为错误红字，其后 24dp 为 48dp 通栏主按钮。无「上一步」：返回即弹出「放弃修改？」，放弃后整个流程作废。凭据过期（服务端或本地 10 分钟计时）退回第 1 步并提示“验证已过期，请重新验证”。成功显示“邮箱已修改 / 请用新邮箱登录”横幅并返回。
- **登录失效（已实现）**：本机当前令牌被服务端拒绝时，退出登录（保留本机数据），显示 important 横幅“登录已失效 / 请重新登录，本机数据不受影响”，并回到欢迎页；此时编辑页的离开保护直接放行，不弹出「放弃修改？」。
- **离开确认弹窗（已实现）**：标题「放弃修改？」，正文 14sp 蓝灰“本次修改尚未保存，离开后将丢失。”，12dp 后为「放弃修改」secondary /「继续编辑」primary，等分、间距 10dp、高 48dp；点遮罩等同继续编辑。
- **头像回退（已实现）**：个人资料、用户中心与分类栏头像统一按“本地缓存 → 远程地址 → 默认图标”显示；加载失败时显示各处原有的默认图标样式，不出现空白；云存储关闭时仍显示本地缓存。
- **验证码与密码**：沿用「欢迎、登录与注册页面」的字段规格：验证码输入与发送按钮间隔 10dp，发送按钮宽 120dp、高 48dp；密码显隐图标 20dp、触控区 48×48dp；辅助入口为蓝色文字，触控高至少 44dp，距主按钮 12dp。
- **弹层**：性别选择、头像预览、未保存离开确认一律按 §4 弹窗外壳（`AppModal`，24dp 外边距/内边距/圆角，最大宽 440dp）；选择列表按 §5（行 ≥56dp，选中浅蓝底 + 20dp 主色勾）；按钮按 §6（`DialogButton` 高 48dp，同容器左右间距 10dp，内容到按钮 12dp）。未保存离开确认为「放弃修改」secondary + 「继续编辑」primary。头像预览区为正方形，最大 280dp。
- **头像操作菜单**：复用锚点气泡菜单，宽 232dp，高度按实际行数计算（不设会裁切动作的固定上限）；依次为查看头像、从相册选择、拍照，行 ≥56dp、左右 16dp、垂直 12dp、图标 22dp 到文字 12dp，行间分割线缩进 16dp；无头像时「查看头像」灰色禁用；点菜单外关闭，不设取消行。菜单关闭后间隔 300ms 再打开预览或系统选图，选择取消不提示。
- **头像预览**：§4 弹窗外壳；标题「查看头像」或「确认新头像」，标题下 12dp 为圆形预览（卡片内宽、最大 280dp，居中，无头像时显示 64dp 默认图标）。查看模式：12dp 后为通栏「关闭」secondary，点遮罩或返回关闭。待确认模式：12dp 后为「重新选择」secondary /「使用此头像」primary 等分，间距 10dp、高 48dp；点遮罩不关闭，系统返回放弃所选图片；上传中右侧按钮显示「上传中…」与白色小指示器，两按钮禁用，预览叠加半透明遮罩与指示器，遮罩与返回均不响应；失败时按钮上方 12dp 显示 14sp 红色错误（`accessibilityRole="alert"`），保留所选图片可重试；成功关闭弹窗并显示「头像已更新」横幅。重新选择沿用同一来源，取消时恢复原待确认图片。
- **状态**：加载使用 §7 规格，不先闪现“未设置”；读取失败保留缓存资料并提示“资料可能不是最新”，提供 text 变体重试。上传/保存中原位显示指示器并锁定重复提交，不显示无真实来源的百分比。
- 当前仅浅色；按压透明度 0.85；所有可点击项提供 `accessibilityRole`、可访问名称与禁用状态。

## 8. 落地状态与迁移路线

| 界面 | 状态 |
| --- | --- |
| 草稿箱弹窗（draft-list-modal） | ✅ 1.0 已落地（本规范首个实现） |
| 新建笔记三个草稿弹窗（new-note-editor） | ✅ 1.0 随外壳一并落地 |
| 编辑页 NoteEditor 恢复/放弃对话框 | ❌ 未迁移（仍是 @expo/ui Button + 旧样式） |
| 新建分类（CreateCategoryModal） | ✅ 已迁移，含图标选择面板展开/收起 |
| 分类操作与删除弹窗 | ✅ 已迁移，覆盖改名、图标网格和右滑删除引导 |
| 笔记操作、分享格式与图片设置 | ✅ 已迁移，含置顶与标星状态操作 |
| 删除确认弹窗（笔记/分类共享 DeleteConfirmDialog） | ✅ 已迁移，含 2 秒倒计时与弹窗内错误反馈 |
| 全局横幅通知 | ✅ 已实现，自动上传队列复用稳定 ID 原位更新 |
| 同步暂存列表 | ✅ 已实现，真机网络切换与布局待用户验收 |
| 个人资料（入口、资料总览、编辑页与弹层） | 🚧 入口、总览、平台占位、头像更换、用户名/简介/性别编辑、地区选择已实现（待真机验收）；邮箱与密码等待 B3 |

迁移原则：接触某弹窗时按本规范重写；新弹窗一律按本规范实现。深色模式、squircle（`react-native-figma-squircle`，需 dev build 重建）在 `docs/待办/TODO.md` §2 立项后另行推进。
