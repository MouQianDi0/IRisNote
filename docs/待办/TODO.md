# IRisNote 待办清单（TODO）

> 生成时间：2026-09-10 08:50:00 +08:00
> 数据来源：通读 `docs/` 全部文档（编辑器进度 0.4.10、核心架构与实施计划、阶段 0–3 验证清单、后续开发指南、样式开发规范等）并与源码实际状态交叉核对。
> 维护约定：本文件是**待办总索引**，不替代 `docs/进度与验证/项目编辑器进度.md`（编辑器进度的权威记录）与 `CHANGELOG.md`（变更流水）。完成一项就把对应条目标记 `[x]` 并注明日期；新增待办同步登记来源。
> 优先级：高 = 当前主线或存在风险；中 = 计划内下一步；低 = 可延后/择机处理。

---

## 1. 编辑器与数据层（当前主线）

### 1.1 阶段 3A 收尾（高）

- [ ] **对账分支实机验证**：杀应用重开，内容一致不产生新版本；另一设备改动后对账追加 `server-reconcile` 版本（阶段 3 清单 §4 唯二未勾选项之一）
- [ ] **Web（SQLite Alpha）迁移与保存链路验证**：v3→v4 迁移、保存建版本、无变化不建版本在 Web 端复验（阶段 3 清单 §4）
- [ ] 真实服务端**拒绝 / 超时路径**验证：云端拒绝或结果未知时草稿保留、不误清理（进度文档 0.4.6 遗留，"接受"路径已于 2026-09-09 验证通过）
- [ ] 账户切换后请求身份验证（旧 token 不应命中新账户数据，进度文档 0.4.6 遗留）
- [ ] 草稿恢复 11 个 label 按钮实机复测（2026-09-08 已统一改 label，未复测）

### 1.2 阶段 3B：历史版本 UI（高，未开始）

- [ ] 详情页历史入口
- [ ] 版本列表 UI
- [ ] 版本内容查看 / 对比
- [ ] 恢复确认流程 + 恢复后的云端上传编排（数据能力 `restoreLocalNoteToRevision` 已就绪，只缺 UI）
- [ ] 快照存储增长观察：当前每次有效保存存整篇快照且不限量（哈希去重属阶段 6；保留策略属待决策 O-005）

### 1.3 用户验收（高，按 D-015 约定 UI 由用户验收）

- [ ] 新建笔记草稿流程 UI 验收（2026-09-10 提交 65007f6，逻辑 57 项 Node 测试通过，视觉效果待用户确认）
- [ ] 查看页四区浮动工具栏交互系统验收（滚动显隐、键盘常驻条切换、防重复点击、focusContent 聚焦）
- [ ] 查看页底部浮动编辑入口与滚动末尾留白
- [ ] 下拉刷新笔记 UI（2026-09-10 提交 3628786）
- [ ] 缓存命中与退出登录清空的 setState 微任务调度，快路径观感确认（进度文档 0.4.4）

### 1.4 外围验证（中）

- [ ] iOS 平台：编辑器、草稿、保存链路（全部历史记录均标注 iOS 未测）
- [ ] 中文输入法组合文本（阶段 2 遗留）
- [ ] 低端 Android 性能基线（阶段 0 遗留，同时是暂定参数 P-001/P-002 复核的前置）
- [ ] 服务端源码审计（阶段 0 遗留）
- [ ] 阶段 1 外部验证项（静态 / Bundle / Web / 真机）——按用户决定"暂时收口"，保留待办不催办
- [ ] 磁盘故障反馈路径验证（进度文档 0.4.6 遗留）

### 1.5 后续阶段（低，按总路线图推进）

- [ ] 阶段 4：基础块数据模型（paragraph / heading / code，块级脏标记与撤销）
- [ ] 阶段 5：旧笔记迁移为块结构
- [ ] 阶段 6：哈希与版本清单
- [ ] 阶段 7：手动增量同步（`baseRevision` 条件提交需服务端支持，见 §5）
- [ ] 阶段 8–12：容量配额 / 附件图片 / 画布图层 / 自动同步 / 高级冲突研究

---

## 2. UI 与设计体系

- [ ] **深色模式**：当前完全缺失（`global.css` 与 `src/shared/theme` 均只有浅色值，源码 0 个 `dark:` 类），但 `docs/UI/样式开发规范.md` §10 已要求"Light/Dark 系统主题下的可读性"检查——规范与现状矛盾，需决策：补实现或修订规范（高）
- [ ] **HyperOS 风格采用决策**：`docs/UI/miuix设计参考（HyperOS风格）.md` 已就绪；若采用需立项 token 语义化改造（含深色分层"卡片亮于背景"、文字透明度阶梯），主色 `#007aff` vs `#3482FF` 的取舍是产品决策（中）
- [ ] 颜色 token 语义化：业务命名（`note-card`、`category-button`、`settings-background`）逐步迁移为语义角色命名，并统一 6/8 位十六进制混写与大小写（中）
- [ ] `spacing.ts` / `typography.ts` token 推广：已定义但业务未普遍采用，页面仍散用 Tailwind 任意值（低）
- [ ] NotesScreen "loading 未使用警告" 与 loading 死状态复核（进度文档 0.4.4 遗留，低）

---

## 3. 业务功能接入（三个 Tab 均为占位页）

源码核实：`todos` / `excerpts` 仍只有 Screen 骨架；`settings` 已完成首页 UI 与现有账户能力接线，但仍无设置数据层和状态管理。

- [x] **Todo**：本地持久化、类型/API 层、创建表单、列表状态、完成与同步冲突处理已实现（2026-09-20）；生产数据库迁移、后端部署、功能门启用与真实 APK 联调见 [Todo 前后端交接与验收](./Todo前后端交接与验收.md)。
- [ ] **Excerpt**：剪贴板服务 `clipboard.service.ts`、`excerpts.api.ts`、粘贴预览与保存（后续开发指南 §13）
- [x] **Settings 首页 UI**：账户卡片、四格状态概览、分组设置、头像入口、版本信息与退出登录（2026-09-14）
- [ ] **Settings 功能接入**：主题、编辑阅读、通知、同步、数据与隐私设置的类型、本地存储服务与子页面；确认实时共享需求后再评估 Provider/Store（后续开发指南 §14）

---

## 4. 工程与配置（后续开发指南 §16 已登记，均需单独立项）

- [ ] `package.json` 包名 `nextnote` 与应用名 IRisNote 不一致（低）
- [ ] `app.json` Image Picker 权限文案仍含 "NextNote"（低）
- [ ] `app.json` 引用不存在的 `assets/images/splash-icon.png`（低，影响导出）
- [ ] `npm run reset-project` 指向不存在的 `scripts/reset-project.js`，避免误用（低）
- [ ] `.env.local` 的 `EXPO_PUBLIC_BASE_URL_LOCAL` 无消费方，Client 只读 `EXPO_PUBLIC_BASE_URL`（低）
- [ ] 分类删除由前端分批删除笔记，缺服务端事务保证（中，数据一致性风险）
- [ ] Zustand 已安装未使用，保留观察或移除（低）

---

## 5. 服务端与运维（irisapi，独立仓库）

- [ ] notes 表无 `updated_at` 列：影响增量同步与排序能力，阶段 7 前需评估补列（中）
- [ ] 服务端 Revision / `baseRevision` 条件更新 / 增量对象协议支持评估（阶段 7 前置，对应待决策 O-002）

---

## 6. 待决策清单（阻塞对应后续阶段，决策人：用户）

| 编号 | 问题 | 前置条件 | 来源 |
| --- | --- | --- | --- |
| O-001 | 是否实施端到端加密 | 威胁模型、密钥恢复、跨设备方案 | 总计划 §4.3 |
| O-002 | 服务端数据库/对象存储/事务方案 | 服务端仓库与部署成本评审 | 总计划 §4.3 |
| O-003 | 画布渲染引擎选型 | Expo SDK 56 兼容性、手写性能、包体 | 总计划 §4.3 |
| O-004 | 块排序字段（整数/分数索引/链式） | 移动块频率与同步合并策略 | 总计划 §4.3 |
| O-005 | 历史版本保留时长与免费/付费差异 | 存储成本与套餐设计（影响 §1.2 快照策略） | 总计划 §4.3 |
| O-006 | 云端草稿是否付费能力 | 隐私、冲突、用户预期 | 总计划 §4.3 |
| O-007 | 多设备同时编辑会话 | 冲突处理成熟度 | 总计划 §4.3 |
| — | Excerpts 是否需要标题/块/附件/草稿/历史 | — | 进度文档 §8 |
| — | 基础块首版类型与排序字段 | 与 O-004 关联 | 进度文档 §8 |
| — | 低端机草稿防抖参数（P-001/P-002）复核 | 低端 Android 实测数据（§1.4） | 进度文档 §8 |
| — | Web 托管平台及 COEP/COOP 配置方式 | 候选平台评审 | 进度文档 §8 |
| — | 深色模式与 HyperOS 风格方向 | §2 前两项 | 本次核对新增 |

---

## 完成记录

（完成条目移入此处，格式：`[YYYY-MM-DD] 条目摘要 + 证据链接`）
