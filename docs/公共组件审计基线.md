# IRisNote 公共组件与设计系统审计基线

> 基线日期：2026-09-14  
> 源码范围：`src/**/*.{ts,tsx}`  
> 样式系统：NativeWind 5、Tailwind CSS 4、`src/shared/theme`  
> 复跑命令：`npm run design:audit`

## 0. 真机布局实测验收（uiautomator 对账）

本基线为静态扫描。凡涉及尺寸/间距的以下场景，须先用真机布局实测对账再下结论：

- **截图判读**：判断截图中组件尺寸/间距是否合规，以实测数值为准，不凭目测；
- **提出修改要求**：需求须附实测数据（当前实际值 vs 规范值），不接受"看起来不对"式反馈；
- **验证改动**：改后在真机导出控件树核对 dp 值，配合截图交用户验收。

实测步骤（真机已连接 adb）：

```bash
node scripts/ui-dump-parse.mjs   # 一键导出+解析，输出每个元素的 px 与 dp（--serial 指定设备）
adb shell wm density             # 需手动换算时查密度（1dp = 密度/160 px）
```

判读规则：

- dump 坐标是 **px**，须除以 `密度/160` 换算 dp（例：476dpi → 1dp=2.975px）；
- 文本 bounds 是**文字边界不是触摸热区**（例：`min-h-11` 按钮热区 44dp，文字仅约 18dp）；
- 原生树经 RN 扁平化，**看不到组件名与 padding 来源**，定位原因必须回源码；
- 可交互性以 content-desc（对应 accessibilityLabel）与 clickable 计数交叉判断；
- clickable 节点数天然多于 RN 触摸组件数（TextInput 外层容器等也被标记），1:1 对不上属正常；
- **无真机/未连接 adb 时跳过本步骤**，结论退回静态检查与目测，不得编造实测数值。

## 1. 基线摘要

| 项目 | 数量 | 每 100 行源码 | 说明 |
| --- | ---: | ---: | --- |
| TypeScript/TSX 文件 | 232 | — | 包含页面、业务组件和公共组件 |
| 源码行 | 18,009 | — | 后续评分分母 |
| 主题目录外固定颜色 | 14 | 0.1 | 其中 13 处属于分享图片/PDF 独立导出主题 |
| 裸 `fontSize` | 19 | 0.1 | 公共排版尚未收口 |
| 裸 `borderRadius` | 13 | 0.1 | 包含业务特有形状和待迁移语义 |
| 含任意值类名的行 | 66 | 0.4 | 是候选项，不自动等于违规 |
| 含可点击控件的文件 | 33 | — | `Pressable` 或 `TouchableOpacity` |
| 含 `TextInput` 的文件 | 11 | — | 包含普通字段和专用编辑输入 |

评分显示当前 Token 逃逸密度不高，但重复组件的问题较明显：已有公共 `TextField` 没有形成实际入口，按钮、输入框、返回按钮和弹窗操作区仍在业务目录内各自实现。

## 2. 固定颜色例外与待处理项

### 合理例外

- `features/notes/components/NoteShare/NoteShareToImage.tsx`：7 处，属于分享图片纸张主题。
- `features/notes/components/NoteShare/NoteShareToPdf.tsx`：6 处，属于 PDF 导出模板主题。

以上导出内容不自动跟随应用主题；如后续支持分享模板换肤，应建立独立的 `ShareThemePreset`，不能并入应用 UI 主题。

### 应用 UI 待处理

- `features/notes/components/viewer/note-operation-info.tsx`：1 处固定颜色，迁移笔记操作区时改用语义 Token。

## 3. 主要漂移位置

### 裸字号

| 文件 | 命中数 | 后续归属 |
| --- | ---: | --- |
| `NoteShareToImage.tsx` | 4 | 导出模板例外 |
| `Banner/banner-card.tsx` | 4 | `AppText` 或 Banner 私有排版配方 |
| `ReadingNavigationPanel.tsx` | 3 | `AppDialog` 内容排版 |
| `ProgressBubble.tsx` | 3 | ProgressBubble 组件配方 |
| `NotesSyncHeader.tsx` | 2 | `AppText` |

### 裸圆角

| 文件 | 命中数 | 后续归属 |
| --- | ---: | --- |
| `note-operation-info.tsx` | 3 | 标题输入已由 `InputSave` 收口；信息区待后续迁移 |
| `CategoryBar.tsx` | 2 | 分类栏业务配方 |
| `NoteShareToImage.tsx` | 2 | 导出模板例外 |
| `editor-bottom-toolbar.tsx` | 1 | 编辑器浮动工具栏，已使用独立 `radii.editorToolbar = 16dp` |
| `CategoryActionModal.tsx` | 1 | 已迁移到 `radii.iconControl` |

### 任意值类名

| 文件 | 命中数 | 处理原则 |
| --- | ---: | --- |
| `note-statistics-popover.tsx` | 7 | 按统计弹窗整页迁移，不机械替换 |
| `CategoryBar.tsx` | 6 | 保留确有业务尺寸的值，其余进入 Token |
| `NotesScreen.tsx` | 5 | 按首页批次审查 |
| `FloatingMenu.tsx` | 4 | 按导航公共组件批次审查 |
| `CategoryActionModal.tsx` | 4 | 随 AppDialog/Input/IconButton 迁移 |

## 4. 现有公共组件完整性

| 组件 | 当前能力 | 主要缺口 | 决定 |
| --- | --- | --- | --- |
| `Button` | 基础 variant、原生 disabled | 无统一尺寸、加载、文字、圆角、按压和语义禁用色 | 由 `AppButton` 替代并提供迁移兼容 |
| `TextField` | 包装原生 TextInput | 无状态配方、ref 约束、前后内容、错误和只读语义，业务调用少 | 由 `Input` 替代 |
| `InputSave` | 52dp 输入与保存入口、状态锁定、无障碍保存语义 | 仅适用于显式保存行，不接管业务异步与关闭 | 保留；笔记标题已接入 |
| `Card` | surface/muted 变体 | 圆角与内边距仍交给调用方 | 保留并补齐有限变体 |
| `ModalPanel` | 三种固定面板尺寸 | 与新 AppDialog 外壳职责重叠 | 并入或服务 `AppDialog` |
| `AppModal/Overlay` | 处理原生 Modal 层级 | 未统一内容外壳 | 保留为基础设施 |
| `Banner` | 全局通知、操作和加载 | 尚未接入新语义主题 | 保留并迁移主题 |
| `Popover` | 锚点、自适应和动效 | compact 圆角仍用旧 Token | 保留并按组件配方迁移 |
| `ProgressBubble` | 拖动定位、文本和书签 | 裸字号、阴影尚未 Token 化 | 保留；圆角已迁到 16dp |
| `Screen` | 页面容器 | 需审查背景和安全区语义 | 保留 |

## 5. 旧实现到目标组件的迁移表

| 旧实现/位置 | 目标公共组件 | 批次 | 当前状态 |
| --- | --- | ---: | --- |
| `shared/ui/Button` | `AppButton` 兼容入口 | 2 | 待迁移 |
| `features/auth/components/AuthButton` | `AppButton` | 2 | 已改为薄适配，无独立视觉实现 |
| 编辑器 `DialogButton` | `AppButton + DialogActions` | 4 | 待迁移；圆角 Token 已为 16dp |
| `shared/ui/TextField` | `Input` | 3 | 待迁移 |
| `features/auth/components/AuthField` | `FormField + Input` | 3 | 已接入 Input/IconButton/AppText；FormField 待阶段 3 |
| 分类创建/改名 `TextInput` | `FormField + Input` | 3/4 | Input 已迁移；FormField 待阶段 3 |
| 笔记操作标题私有 `TextInput` + 保存按钮 | `InputSave` | 2/7 | 已迁移；自动保存与改名状态机保留在 `NoteRename` |
| 认证、阅读页、编辑器返回入口 | `BackButton` | 2/7 | 认证、`NoteViewerHeader`、`PlainTextEditor` 已迁移；其余入口按域审查 |
| 页面级图标 Pressable | `IconButton` | 2 后分域迁移 | 认证密码显隐已迁移；其余待迁移 |
| `CategoryIconPicker` | `IconPickerGrid + IconPickerDialog` | 5 | 单元圆角已为 16dp，结构待迁移 |
| 分类/草稿/阅读业务弹窗外壳 | `AppDialog` | 4/7 | 待迁移 |
| 各弹窗底部按钮排列 | `DialogActions` | 4 | 待迁移 |
| 分散的加载/空白/错误结构 | `AsyncStateView` | 6 | 待提取 |
| 分散的局部提示 | `InlineMessage` | 6 | 待提取 |
| 重复列表与选择行 | `ListRow/SelectionRow` | 6 | 待提取 |

## 6. 审计命令与判定规则

- `npm run design:audit`：输出总量、逃逸计数和每类前五个文件。
- `npm run theme:check`：检查 `global.css` 托管区块是否与默认主题预设一致。
- `npm run theme:sync`：主题预设修改后重新生成 CSS 托管区块。

审计命中只是候选：

- 导出图片/PDF、光学微调和业务专用尺寸可以保留，但必须记录理由。
- 同一字面值重复出现两次以上，优先提取 Token 或组件配方。
- 页面不得新增公共组件身份色、身份圆角或状态优先级。
- 迁移按业务域小批进行，每批复跑审计；指标原则上应单调下降，新增命中必须有说明。

## 7. 当前结论

- 最高优先级不是清理所有任意值，而是先完成公共文字、按钮、输入框和弹窗入口。
- 主题值已具备单一默认预设与 CSS 一致性检查，旧导出继续保留兼容。
- `hyperControl` 的历史复用已经隔离：公共按钮和编辑器浮动工具栏均为 16dp，但工具栏使用独立 `editorToolbar` 语义，不再借用按钮 Token。
- 认证域已作为第一个迁移样板接入阶段 2 基础组件；后续按分类、草稿、阅读与笔记域推进。

## 8. 阶段 2 首批快照

- 已新增并从 `@/shared/ui` 导出 `AppText`、`AppButton`、`IconButton`、`BackButton`、`Input`、`InputSave`。
- 认证按钮、普通字段、密码显隐和返回入口已接入公共组件；认证校验、自动填充、键盘、系统返回和接口逻辑仍保留在业务层。
- 2026-09-14 复跑审计：242 个源码文件、18,490 行；主题目录外固定颜色仍为 14，裸字号仍为 19，说明本批未新增颜色或字号逃逸。新增组件使可点击控件文件数增至 34；后续按调用方迁移并删除旧兼容实现后再观察收敛值。
- `@expo/ui` Universal 输入需要独立 state/Host 组合；本批为保持标准 `TextInput` ref、键盘和自动填充契约，使用 React Native 原语封装，Picker、Switch、BottomSheet 等后续系统控件仍优先评估 `@expo/ui`。
- 分类新建和重命名字段接入公共 `Input` 后，2026-09-14 复跑审计为 242 个源码文件、18,513 行，含直接 `TextInput` 的文件由阶段 0 的 11 个降至 9 个；固定颜色与裸字号仍分别为 14 和 19。
- 笔记操作标题接入 `InputSave`，公共 `Input` 常驻边框统一提升为 2dp；保存图标使用 `textSecondary` 灰色，页面不再维护保存按钮与聚焦边框的身份样式。
