# 设置与用户页 Design QA

## 设置首页历史验收

- 日期：2026-09-14
- 参考图：`C:\Users\MOUQIA~1\AppData\Local\Temp\codex-clipboard-5b764ed3-d17a-4483-8f36-d2678f85c789.jpg`
- 验收目标：只复用参考图的账户区、四格概览和分组卡片纵向结构；不复用会员业务与原图配色。
- 实际页面：`/pages/user/settings`
- 视觉验收态：使用仅在本轮验证期间存在的本地预览路由注入合成账户，截图完成后已删除该路由。

### 对照结果

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 结构顺序 | 通过 | 顶栏、账户卡片、四格概览、偏好设置、数据与隐私、支持、退出登录依次呈现。 |
| 会员内容排除 | 通过 | 页面没有会员中心、购买入口、套餐、容量进度或轮播圆点。 |
| 品牌与配色 | 通过 | 使用 IRisNote `#007AFF` 作为图标与强调色，页面使用浅灰背景和白色卡片。 |
| 卡片和间距 | 通过 | 16dp 页面边距、16dp 卡片圆角、16–20dp 区块节奏与 56dp 最小设置行符合视觉规范。 |
| 状态真实性 | 通过 | 未实现能力以灰色「规划中」禁用态展示且无箭头；概览项只读。 |
| 账户状态 | 通过 | 已登录合成态显示头像、昵称、邮箱、加入时间；真实未登录态显示说明和登录注册入口。 |
| 390×844 | 通过 | 四格概览保持一行，设置行无水平裁切，页面可滚动到底部退出入口。 |
| 320×568 | 通过 | 账户卡和四格概览保持完整；较长说明自然换行，无水平溢出。 |
| 宽屏 | 通过 | 内容限制在 560dp 并水平居中，卡片层级稳定。 |
| 浏览器日志 | 通过 | 可见验收期间未发现 error 或 warning 级浏览器日志。 |

### 验证边界

- Codex 应用内浏览器不提供 `--headed --persistent` 启动参数；本轮使用可见的应用内浏览器会话完成检查，未声称使用这些参数。
- 未调用真实认证、头像上传或远端同步接口；退出登录只通过代码路径和可访问状态检查，未删除当前用户的真实会话。
- Android/iOS 真机的系统返回、相册/相机权限和安全区仍需真机验收。

final result: passed

---

## 用户页移动端验收标准

- 状态：标准已更新；代码静态检查与移动端 Bundle 由开发侧执行，最终视觉与系统交互由 Android/iOS 实机验收。
- 实际页面：`/(tabs)/user`
- 产品范围：移动端；Web 宽屏、桌面居中和 320dp 超小视口不参与用户页设计验收。
- 基准竖屏：390×844dp、412×915dp。
- 当前例外：暂时忽略右侧悬浮 Tab 栏与用户页内容重叠，不得通过压缩用户页宽度或增加底部留白规避。

### 自动检查

| 检查项 | 验收标准 |
| --- | --- |
| 类型与规范 | 全量 TypeScript、用户页相关 ESLint、`git diff --check` 全部通过。 |
| 数据逻辑 | 概览合并、分账号阅读记录与继续阅读选择的 Node 测试通过。 |
| Android Bundle | Expo Android 生产导出通过，无用户页编译错误。 |
| iOS Bundle | Expo iOS 生产导出通过，无用户页编译错误；Windows 上的 Bundle 不等于 iOS 真机验收。 |

### 移动端视觉与交互

| 检查项 | 验收标准 |
| --- | --- |
| 全屏宽度 | 页面和滚动内容占满手机可用宽度；所有主卡片距左右边缘各 16dp，不设最大宽度，不预留 Tab 轨道。 |
| 全屏高度 | 页面和滚动内容最小高度等于手机可用高度；内容不足一屏时顶部账户区、中部继续阅读、底部我的内容弹性分布，底部保留 16dp。 |
| 超高内容 | 动态字体、长文本或系统安全区使内容超过一屏时可以自然滚动，不裁切最后一行。 |
| 账户卡 | 无顶栏；头像 64×64dp、昵称 20sp、邮箱和加入时间 13sp，长文本单行截断，上传状态原位反馈。 |
| 内容概览 | 笔记、分类、标星三栏等宽；加载显示指示器，不可用显示“—”，不得出现演示数字。 |
| 继续阅读 | 有记录时显示真实标题和进度；无记录时显示空状态并可进入全部笔记。 |
| 快捷入口 | 全部笔记、星标笔记、草稿箱均可触达真实目标；关闭草稿箱后一次性参数被清除。 |
| 触控与读屏 | 可点击区域具有正确的可访问名称与角色，按压反馈明确；头像、齿轮和三条快捷入口可操作。 |

### 实机边界

- Android/iOS 必须分别检查状态栏与底部安全区、纵向滚动、头像相册/相机权限、返回行为和真实账户数据。
- Android/iOS Bundle、Node 测试和静态检查只能证明可构建与纯逻辑正确，不能替代实机视觉、权限或真实接口验收。
- 用户页不再要求 Web 宽屏截图、Web 浏览器交互或 320×568 适配结果。
